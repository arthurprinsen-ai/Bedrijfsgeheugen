export function checkTrace(events=[]){const violations=[];const types=new Set(events.map(e=>e.type));if(types.has('Mission')&&!types.has('Outcome'))violations.push('MISSING_OUTCOME');const traces=new Set(events.map(e=>e.trace_id).filter(Boolean));if(traces.size>1)violations.push('BROKEN_TRACE_LINEAGE');return {ok:violations.length===0,violations};}
export function checkInvariants(s){const v=[];if(Number(s.current_state_age_hours||0)>Number(s.max_state_age_hours||Infinity))v.push('STALE_CURRENT_STATE');const keys=s.idempotency_keys||[];if(new Set(keys).size!==keys.length)v.push('DUPLICATE_IDEMPOTENCY_KEY');if(s.production_status==='PRODUCTION_GREEN'&&(!s.exact_production_sha||s.verified_production!==true))v.push('INVALID_PRODUCTION_GREEN');if(Number(s.heartbeat_age_minutes||0)>Number(s.max_heartbeat_minutes||Infinity))v.push('MISSING_HEARTBEAT');if(s.parent_lineage_valid===false)v.push('BROKEN_PARENT_LINEAGE');return {ok:v.length===0,violations:v};}
