{
  "fingerprint": "delivery-browser-visibility-boundedness-v1",
  "date": "2026-09-24",
  "type": "CI_BROWSER_GATE_BOUNDEDNESS_RECOVERY",
  "status": "IMPLEMENTED_CANDIDATE",
  "terminal_state": "CANDIDATE_UNTIL_PROTECTED_MERGE_AND_PRODUCTION_READBACK",
  "problem": "The Required website browser lane could remain in progress because standalone visibility verification contained an unbounded document.fonts.ready wait and overly expensive retry envelopes across the full sitemap.",
  "root_cause": "The browser verifier relied on a job-level timeout but did not bound every asynchronous wait inside the route loop. A single unresolved browser promise could consume the runner until the outer job timeout.",
  "fix": [
    "Bound document.fonts.ready with a deterministic timeout",
    "Reduce navigation retries and navigation timeout",
    "Bound sitemap fetch",
    "Add a total fail-closed sweep budget",
    "Preserve product assertions while preventing runner leaks"
  ],
  "prevention_rule": "Every browser/readback loop must bound network navigation, page-side async waits and total sweep runtime; outer job timeout alone is not sufficient.",
  "compiler": {
    "failure_class": "UNBOUNDED_BROWSER_ASYNC_WAIT",
    "scope": "CI_WEBSITE_BROWSER_GATE",
    "machine_enforceable": true,
    "repeat_count": 1,
    "security_sensitive": false
  },
  "evaluation": {
    "historical_replay": [
      "tests/brain-standalone-visibility-boundedness-v1.test.mjs"
    ],
    "invariants": [
      "each navigation attempt has a finite timeout",
      "page-side font readiness has a finite timeout",
      "sitemap retrieval has a finite timeout",
      "the full visibility sweep has a total fail-closed runtime budget",
      "public-page visibility, occlusion, content-length and CLS assertions remain enforced"
    ]
  },
  "skill_targets": [
    ".agents/skills/powerhouse-browser-gate-boundedness/SKILL.md"
  ],
  "evidence": {
    "pr": 2834,
    "script": "tools/site-shell/standalone-visibility-check.mjs",
    "regression": "tests/brain-standalone-visibility-boundedness-v1.test.mjs",
    "skill": ".agents/skills/powerhouse-browser-gate-boundedness/SKILL.md"
  }
}
