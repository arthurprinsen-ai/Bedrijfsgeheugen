export function missionBudget(m){const v=Math.max(0,Number(m.expected_value)||0),c=Math.max(.1,Math.min(1,Number(m.confidence)||.5));return {max_credits:Math.max(2,Math.min(200,Math.round(v*.08*c)+2)),max_external_calls:Math.max(1,Math.min(20,Math.ceil(v/50))),max_ai_steps:Math.max(1,Math.min(8,Math.ceil(v/100))),max_runtime_seconds:Math.max(30,Math.min(900,Math.ceil(v*1.5))),max_retries:2};}
export function costPerOutcome({cost=0,useful_outcomes=0}){return useful_outcomes>0?Math.round((Number(cost)/Number(useful_outcomes))*100)/100:null}
export function detectCostAnomaly(x){const ratio=(Number(x.credits_per_run)||0)/Math.max(.01,Number(x.baseline_credits_per_run)||.01);const retryStorm=Number(x.retry_count||0)>2;return {anomaly:ratio>=2||retryStorm,ratio:Math.round(ratio*100)/100,retry_storm:retryStorm};}
