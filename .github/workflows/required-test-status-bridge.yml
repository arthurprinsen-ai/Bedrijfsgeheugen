name: Required test status bridge

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  statuses: write

jobs:
  publish-required-status:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy bridge is manual-only after canonical test cutover
        run: echo "Required test publishes the branch-protected test check directly; no polling PR runner is required."
