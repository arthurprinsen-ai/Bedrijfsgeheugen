name: Required test status bridge

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  statuses: write

jobs:
  publish-required-status:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy bridge is manual-only after single-flight cutover
        run: echo "The branch-protected test context is now emitted directly by Required test."
