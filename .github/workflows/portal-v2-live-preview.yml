name: Portal V2 Live Preview
on:
  workflow_dispatch:
permissions: {contents: read, statuses: read}
jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Portal V2 exact PR preview checks are owned by Required test single-flight.'
