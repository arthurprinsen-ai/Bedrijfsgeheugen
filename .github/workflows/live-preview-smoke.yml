name: Live Preview Smoke
on:
  workflow_dispatch:
permissions: {contents: read, statuses: read}
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Exact PR Netlify preview smoke is executed inside Required test single-flight.'
