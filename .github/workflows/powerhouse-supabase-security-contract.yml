name: Powerhouse Supabase Security Contract

on:
  pull_request:
    paths:
      - 'supabase/migrations/**'
      - 'scripts/check_powerhouse_supabase_security.py'
      - '.github/workflows/powerhouse-supabase-security-contract.yml'
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
      - 'scripts/check_powerhouse_supabase_security.py'
      - '.github/workflows/powerhouse-supabase-security-contract.yml'

permissions:
  contents: read

jobs:
  supabase-security-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Resolve comparison base
        id: base
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "base=${{ github.event.pull_request.base.sha }}" >> "$GITHUB_OUTPUT"
            echo "head=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_OUTPUT"
          else
            echo "base=${{ github.event.before }}" >> "$GITHUB_OUTPUT"
            echo "head=${{ github.sha }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Enforce fail-closed Supabase migration security contract
        run: python3 scripts/check_powerhouse_supabase_security.py "${{ steps.base.outputs.base }}" "${{ steps.base.outputs.head }}"
