name: Business OS Live Preview
on:
  workflow_dispatch:
permissions: {contents: read, statuses: read}
jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PR preview verification is owned by Required test single-flight; this manual workflow remains diagnostic-only.'
