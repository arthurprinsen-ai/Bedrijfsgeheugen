name: Repository Writer Operational Verification

on:
  push:
    branches:
      - 'verify/menu-balk-fix-*'
      - 'verify/regelgeving-bijwerken-*'
      - 'verify/seo-controle-*'
      - 'verify/paginacontrole-*'
      - 'verify/approved-central-blog-*'
      - 'verify/blog-bijwerken-*'
      - 'verify/weekblog-*'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  actions: write

concurrency:
  group: repo-writer-operational-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  dispatch-writer-candidate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ github.token }}
      VERIFY_REF: ${{ github.ref_name }}
      VERIFY_SHA: ${{ github.sha }}
    steps:
      - name: Resolve writer contract
        id: writer
        shell: bash
        run: |
          set -euo pipefail
          case "$VERIFY_REF" in
            verify/menu-balk-fix-*) WRITER='menu-balk-fix'; WORKFLOW='menu-balk-fix.yml'; GENERIC_SHADOW='false'; EXTRA='none' ;;
            verify/regelgeving-bijwerken-*) WRITER='regelgeving-bijwerken'; WORKFLOW='regelgeving-bijwerken.yml'; GENERIC_SHADOW='true'; EXTRA='none' ;;
            verify/seo-controle-*) WRITER='seo-controle'; WORKFLOW='seo-controle.yml'; GENERIC_SHADOW='true'; EXTRA='seo-all' ;;
            verify/paginacontrole-*) WRITER='paginacontrole'; WORKFLOW='paginacontrole.yml'; GENERIC_SHADOW='true'; EXTRA='none' ;;
            verify/approved-central-blog-*) WRITER='approved-central-blog'; WORKFLOW='approved-central-blog.yml'; GENERIC_SHADOW='false'; EXTRA='verification' ;;
            verify/blog-bijwerken-*) WRITER='blog-bijwerken'; WORKFLOW='blog-bijwerken.yml'; GENERIC_SHADOW='false'; EXTRA='verification' ;;
            verify/weekblog-*) WRITER='weekblog'; WORKFLOW='weekblog.yml'; GENERIC_SHADOW='true'; EXTRA='none' ;;
            *) echo "::error::UNSUPPORTED_VERIFY_REF:$VERIFY_REF"; exit 1 ;;
          esac
          echo "writer=$WRITER" >> "$GITHUB_OUTPUT"
          echo "workflow=$WORKFLOW" >> "$GITHUB_OUTPUT"
          echo "generic_shadow=$GENERIC_SHADOW" >> "$GITHUB_OUTPUT"
          echo "extra=$EXTRA" >> "$GITHUB_OUTPUT"
      - name: Resolve exact verification PR and current-main compatibility
        id: verify
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="$(gh pr list --repo "$GITHUB_REPOSITORY" --base main --head "$VERIFY_REF" --state open --limit 2 --json number --jq '.[0].number // empty')"
          [ -n "$PR_NUMBER" ] || { echo "::error::VERIFY_PR_NOT_FOUND:$VERIFY_REF"; exit 1; }
          EXPECTED_MAIN_SHA="$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" --jq '.base.sha')"
          actual_main_sha="$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/heads/main" --jq '.object.sha')"
          if [ "$actual_main_sha" != "$EXPECTED_MAIN_SHA" ]; then
            pr_files="$(gh pr diff "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --name-only | sort -u)"
            main_files="$(gh api "repos/${GITHUB_REPOSITORY}/compare/${EXPECTED_MAIN_SHA}...${actual_main_sha}" --jq '.files[].filename' | sort -u)"
            overlap="$(comm -12 <(printf '%s\n' "$pr_files" | sed '/^$/d') <(printf '%s\n' "$main_files" | sed '/^$/d') || true)"
            [ -z "$overlap" ] || { echo "::error::CURRENT_MAIN_SCOPE_OVERLAP:$EXPECTED_MAIN_SHA:$actual_main_sha"; printf '%s\n' "$overlap"; exit 1; }
          fi
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          echo "base_sha=$EXPECTED_MAIN_SHA" >> "$GITHUB_OUTPUT"
      - name: Dispatch real writer in candidate mode only
        shell: bash
        env:
          WORKFLOW: ${{ steps.writer.outputs.workflow }}
          EXTRA: ${{ steps.writer.outputs.extra }}
        run: |
          set -euo pipefail
          args=(workflow run "$WORKFLOW" --repo "$GITHUB_REPOSITORY" --ref "$VERIFY_REF" -f delivery_mode=candidate-pr)
          case "$EXTRA" in
            none) ;;
            seo-all) args+=(-f alles=true) ;;
            verification) args+=(-f verification_mode=true) ;;
            *) echo "::error::UNSUPPORTED_WRITER_EXTRA:$EXTRA"; exit 1 ;;
          esac
          gh "${args[@]}"
      - name: Resolve exact writer candidate PR
        id: candidate
        if: steps.writer.outputs.generic_shadow == 'true'
        shell: bash
        env:
          WRITER: ${{ steps.writer.outputs.writer }}
        run: |
          set -euo pipefail
          PR_NUMBER=""
          for attempt in $(seq 1 72); do
            matches="$(gh pr list --repo "$GITHUB_REPOSITORY" --base main --state open --limit 100 --json number,headRefName,body | jq -r --arg prefix "writer/$WRITER/" --arg verify "$VERIFY_SHA" '.[] | select(.headRefName | startswith($prefix)) | select((.body // "") | contains($verify)) | .number')"
            count="$(printf '%s\n' "$matches" | sed '/^$/d' | wc -l | tr -d ' ')"
            [ "$count" -le 1 ] || { echo "::error::AMBIGUOUS_WRITER_PR:$WRITER:$VERIFY_SHA:$matches"; exit 1; }
            if [ "$count" -eq 1 ]; then PR_NUMBER="$(printf '%s\n' "$matches" | sed '/^$/d')"; break; fi
            sleep 5
          done
          [ -n "$PR_NUMBER" ] || { echo "::error::WRITER_PR_NOT_FOUND:$WRITER:$VERIFY_SHA"; exit 1; }
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          echo "base_sha=$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" --jq '.base.sha')" >> "$GITHUB_OUTPUT"
          echo "head_sha=$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" --jq '.head.sha')" >> "$GITHUB_OUTPUT"
          echo "head_ref=$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" --jq '.head.ref')" >> "$GITHUB_OUTPUT"
      - name: Dispatch immutable read-only shadow
        if: steps.writer.outputs.generic_shadow == 'true'
        shell: bash
        env:
          PR_NUMBER: ${{ steps.candidate.outputs.pr_number }}
          PR_BASE_SHA: ${{ steps.candidate.outputs.base_sha }}
          PR_HEAD_SHA: ${{ steps.candidate.outputs.head_sha }}
          PR_HEAD_REF: ${{ steps.candidate.outputs.head_ref }}
        run: |
          set -euo pipefail
          gh workflow run repo-writer-candidate-shadow.yml --repo "$GITHUB_REPOSITORY" --ref main -f pr_number="$PR_NUMBER" -f base_sha="$PR_BASE_SHA" -f head_sha="$PR_HEAD_SHA" -f candidate_branch="$PR_HEAD_REF"
