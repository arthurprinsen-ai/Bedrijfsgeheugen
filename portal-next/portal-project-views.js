if(typeof window!=='undefined'&&typeof customElements!=='undefined') import('./connector-builder-element.js');

const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const money=value=>Number.isFinite(Number(value))?new Intl.NumberFormat('nl-NL',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(value)):'Niet beschikbaar';
const status=value=>esc(value||'Gepland');
const partById=(project,id)=>project?.parts?.find(part=>part.id===id);

function empty(message){return `<section class="project-empty" role="status"><strong>Geen gegevens beschikbaar</strong><p>${esc(message)}</p></section>`;}
function quoteSummary(project){const q=project.quote||{};return `<section class="project-summary-grid"><article><span>Klant</span><strong>${esc(project.customer?.name||'Niet beschikbaar')}</strong></article><article><span>Offerte</span><strong>${esc(q.number||'Niet beschikbaar')}</strong></article><article><span>Status</span><strong>${status(q.status)}</strong></article><article><span>Waarde</span><strong>${money(q.amount)}</strong></article><article><span>Geldig tot</span><strong>${esc(q.validUntil||'Niet beschikbaar')}</strong></article><article><span>Akkoord</span><strong>${q.signed===true?'Getekend':q.signed===false?'Niet getekend':'Niet beschikbaar'}</strong></article></section>`;}
function storyHtml(story){return `<article class="project-story" id="${esc(story.id)}"><span>User story</span><p><b>${esc(story.role||'Rol niet beschikbaar')}</b> wil <b>${esc(story.need||'behoefte niet beschikbaar')}</b>, zodat <b>${esc(story.outcome||'resultaat niet beschikbaar')}</b>.</p><small>Status: Nog niet gestart</small></article>`;}
function sprintHtml(sprint,project){const stories=(project.stories||[]).filter(s=>s.partId===sprint.partId);return `<article class="project-sprint" id="${esc(sprint.id)}"><header><div><span>Sprint · ${status(sprint.status)}</span><h4>${esc(sprint.title||'Sprint')}</h4></div></header><dl><div><dt>Wat doen we</dt><dd>${esc(sprint.work||'Niet beschikbaar')}</dd></div><div><dt>Wat leveren we op</dt><dd>${esc(sprint.deliverable||'Niet beschikbaar')}</dd></div></dl>${stories.length?`<div class="project-story-list">${stories.map(storyHtml).join('')}</div>`:''}</article>`;}
function partHtml(part,project){const sprints=(project.sprints||[]).filter(s=>s.partId===part.id);const stories=(project.stories||[]).filter(s=>s.partId===part.id);return `<article class="project-part" id="${esc(part.id)}"><header><div><span>${part.required?'Vast onderdeel':'Optioneel onderdeel'} · ${esc(part.weeks||0)} weken</span><h3>${esc(part.title)}</h3><p>${esc(part.summary||'')}</p></div><strong>${money(part.price)}</strong></header><div class="project-part-meta"><span>Type: ${esc(part.kind)}</span><span>Status: Gepland</span></div>${sprints.length?`<section><h4>Sprints</h4><div class="project-sprint-list">${sprints.map(s=>sprintHtml(s,project)).join('')}</div></section>`:''}${!sprints.length&&stories.length?`<section><h4>User stories</h4><div class="project-story-list">${stories.map(storyHtml).join('')}</div></section>`:''}</article>`;}
function renderOfferte(project){if(!project?.quote)return empty('Er is voor deze klant geen offerte beschikbaar.');return `<section class="project-native-view" data-project-view="offerte"><header class="project-view-head"><div><span class="eyebrow">Commerciële scope en afspraken</span><h3>${esc(project.quote.title||'Offerte')}</h3><p>De offerte is de bron voor onderdelen, sprints, stories, deliverables, koppelingen en planning.</p></div></header>${quoteSummary(project)}<section class="project-parts"><div class="project-section-title"><span class="eyebrow">Scope</span><h3>Onderdelen, sprints en user stories</h3></div>${project.parts?.length?project.parts.map(p=>partHtml(p,project)).join(''):empty('De offerte bevat geen onderdelen.')}</section>${renderCommercialDetails(project)}</section>`;}
function renderCommercialDetails(project){const q=project.quote?.raw||{};const needs=project.customerNeeds||[];return `<section class="project-commercial-details"><div class="project-section-title"><span class="eyebrow">Afspraken</span><h3>Licenties, klantinput en doorlopend</h3></div><div class="project-detail-grid"><article><h4>Licenties / externe kosten</h4><pre>${esc(typeof project.licenses==='string'?project.licenses:project.licenses?JSON.stringify(project.licenses,null,2):'Niet beschikbaar')}</pre></article><article><h4>Van klant nodig</h4>${needs.length?`<ul>${needs.map(n=>`<li>${esc(Array.isArray(n)?n.join(' — '):typeof n==='object'?JSON.stringify(n):n)}</li>`).join('')}</ul>`:'<p>Niet beschikbaar</p>'}</article><article><h4>Architectuur</h4><pre>${esc(project.architecture?typeof project.architecture==='string'?project.architecture:JSON.stringify(project.architecture,null,2):'Niet beschikbaar')}</pre></article><article><h4>Doorlopend</h4><pre>${esc(project.subscription?typeof project.subscription==='string'?project.subscription:JSON.stringify(project.subscription,null,2):'Niet beschikbaar')}</pre></article></div>${q.vastePrijs?`<p class="project-note">${esc(q.vastePrijs)}</p>`:''}</section>`;}
function renderRoadmap(project){const planning=project?.planning||[];const parts=project?.parts||[];return `<section class="project-native-view" data-project-view="roadmap"><div class="project-section-title"><span class="eyebrow">Planning en uitvoering</span><h3>Roadmap</h3><p>Gepland uit de offerte; daadwerkelijke voortgang wordt alleen met runtime-evidence gepromoveerd.</p></div><div class="project-roadmap">${planning.length?planning.map(item=>`<article><span>${status(item.status)}</span><h4>${esc(item.label||'Mijlpaal')}</h4><p>${esc(item.detail||'')}</p></article>`).join(''):parts.map(part=>`<article><span>Gepland</span><h4>${esc(part.title)}</h4><p>${esc(part.weeks)} weken · ${money(part.price)}</p></article>`).join('')||empty('Geen planning beschikbaar.')}</div></section>`;}
function renderTasks(project){const stories=project?.stories||[];return `<section class="project-native-view" data-project-view="taken-werkstromen"><div class="project-section-title"><span class="eyebrow">Uitvoering</span><h3>Taken & werkstromen</h3><p>Offerte-stories zijn geplande behoeften, geen verzonnen actieve taken. Eigenaar en uitvoering verschijnen pas uit project/runtimedata.</p></div><div class="project-task-list">${stories.length?stories.map(story=>{const part=partById(project,story.partId);return `<article class="project-task"><span>Nog niet gestart</span><h4>${esc(story.need||'Geplande behoefte')}</h4><p>${esc(story.role||'Rol niet beschikbaar')} → ${esc(story.outcome||'Resultaat niet beschikbaar')}</p><small>Onderdeel: ${esc(part?.title||story.partId)}</small></article>`}).join(''):empty('Er zijn geen uitvoeringsitems beschikbaar.')}</div></section>`;}
function renderDocuments(project){const docs=project?.documents||[];return `<section class="project-native-view" data-project-view="documenten"><div class="project-section-title"><span class="eyebrow">Deliverables</span><h3>Documenten</h3></div><div class="project-table-list">${docs.length?docs.map(doc=>{const part=partById(project,doc.partId);return `<article><div><strong>${esc(doc.name||'Document')}</strong><small>${esc(part?.title||doc.partId)}</small></div><span>Week ${esc(doc.week??'—')}</span><b>Gepland</b></article>`}).join(''):empty('Geen geplande documenten beschikbaar.')}</div></section>`;}
function renderIntegrations(project){const rows=project?.integrations||[];return `<section class="project-native-view" data-project-view="koppelingen"><div class="project-section-title"><span class="eyebrow">Bronnen en integraties</span><h3>Koppelingen</h3><p>Een koppeling uit de offerte is gepland en wordt pas actief met technische runtime-evidence.</p></div><div class="project-table-list">${rows.length?rows.map(row=>{const part=partById(project,row.partId);return `<article><div><strong>${esc(row.name||'Koppeling')}</strong><small>${esc(row.purpose||'Doel niet beschikbaar')} · ${esc(part?.title||row.partId)}</small></div><span>Week ${esc(row.week??'—')}</span><b>Gepland</b></article>`}).join(''):empty('Geen geplande koppelingen beschikbaar.')}</div><connector-builder-app></connector-builder-app></section>`;}

export function renderProjectPage(pageId,project){
  if(!project)return empty('Projectdata is niet beschikbaar.');
  if(pageId==='offerte')return renderOfferte(project);
  if(pageId==='roadmap')return renderRoadmap(project);
  if(pageId==='taken-werkstromen')return renderTasks(project);
  if(pageId==='documenten')return renderDocuments(project);
  if(pageId==='koppelingen')return renderIntegrations(project);
  return empty('Voor deze pagina is geen projectweergave beschikbaar.');
}
