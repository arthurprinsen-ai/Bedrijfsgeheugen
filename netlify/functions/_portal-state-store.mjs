const ALLOWED_TOP_LEVEL=new Set(['user','company','period','managementSummary','healthCards','roadmap','recommendedActions','monthlyImpact','activities','integrationStatus','quickLinks','graph','signals','decisions','actions','valueItems','memories','agents','audit','admin','sourceMeta']);
export function portalStateKey(userId){if(!userId||typeof userId!=='string')throw new TypeError('userId required');return`identity/${encodeURIComponent(userId)}/canonical-state`}
export function sanitizePortalState(input){if(!input||typeof input!=='object'||Array.isArray(input))throw new TypeError('portal state must be an object');const clean={};for(const [key,value] of Object.entries(input)){if(ALLOWED_TOP_LEVEL.has(key))clean[key]=value}return clean}
export async function readPortalState(store,userId){return await store.get(portalStateKey(userId),{type:'json',consistency:'strong'})}
export async function writePortalState(store,userId,input,{now=()=>new Date().toISOString()}={}){const state=sanitizePortalState(input);const existing=await readPortalState(store,userId);const revision=Number(existing?.revision||0)+1;const record={schemaVersion:1,revision,state,updatedAt:now()};await store.setJSON(portalStateKey(userId),record);return record}
