[build]
  publish = "."
  command = "echo 'Static preview only'"

[context.deploy-preview]
  publish = "."
  command = "echo 'Static preview only'"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
