import { execFileSync, spawnSync } from 'node:child_process';
import { readFile, writeFile } from 'node:fs/promises';
import { pathToFileURL } from 'node:url';
import { createDeliveryPlan } from '../brain-delivery-system.mjs';
import { deriveRequiredTestSuites } from '../delivery-required-test-suites.mjs';
import { classifyWebsiteRelease } from '../site-shell/website-release-risk.mjs';

const FAILURE_DIAGNOSTIC_PATH = '.single-flight-failure.json';

const SHARED_COMMANDS = Object.freeze([
  { id: 'single-flight-contracts', lane: 'shared', command: 'node', args: ['--test', 'tests/single-flight-release-kernel.test.mjs', 'tests/single-flight-release-kernel-plan.test.mjs', 'tests/single-flight-stale-head-contract.test.mjs'] },
  { id: 'delivery-control-plane', lane: 'shared', command: 'node', args: ['--test', 'tests/brain-change-scoped-release-lanes.test.mjs', 'tests/brain-moving-main-successor-guard.test.mjs', 'tests/brain-composable-release-control-plane.test.mjs'] },
  { id: 'nonterminal-completion-contract', lane: 'shared', command: 'node', args: ['--test', 'tests/agents-delivery-contract.test.mjs'] },
  { id: 'learning-materiality-contract', lane: 'shared', command: 'node', args: ['--test', 'tests/make-agent-learning-promotion-contract.test.mjs', 'tests/chat-learning-completeness-addendum.test.mjs'] },
  { id: 'learning-delivery-classifier', lane: 'shared', command: 'node', args: ['--test', 'tests/brain-learning-contract-delivery-classification.test.mjs'] },
]);

const BACKEND_COMMANDS = Object.freeze([{ id: 'backend-contracts', lane: 'backend', command: 'node', args: ['--test', 'tests/brain-adapter-conformance.test.mjs', 'tests/brain-platform-adapters.test.mjs', 'tests/universal-event-envelope.test.mjs', 'tests/event-retention-policy.test.mjs', 'tests/event-retention-expiry.test.mjs', 'tests/universal-event-contract-check.test.mjs', 'tests/universal-event-adapters.test.mjs', 'tests/universal-failure-learning.test.mjs', 'tests/make-cost-governor-policy.test.mjs', 'tests/make-cost-governance-contract.test.mjs'] }]);
const PORTAL_COMMANDS = Object.freeze([{ id: 'portal-contracts', lane: 'portal', command: 'bash', args: ['-lc', 'node --test tests/portal-*.test.mjs tests/connector-*.test.mjs tests/customer-portal-routing.test.mjs tests/portal-production-contract.test.mjs'] }]);
const AUTOMATION_COMMANDS = Object.freeze([{ id: 'automation-contracts', lane: 'automation', command: 'bash', args: ['-lc', 'node --test tests/delivery-*.test.mjs tests/brain-delivery-system.test.mjs'] }]);
const SHARED_MEMORY_COMMAND = Object.freeze({ id: 'shared-agent-memory-contracts', lane: 'shared', command: 'bash', args: ['-lc', 'node --test tests/development-doc-contract.test.mjs tests/delivery-chat-*.test.mjs tests/team-learning-event.test.mjs tests/team-memory-fingerprint.test.mjs tests/opportunity-scout.test.mjs tests/production-promotion-controller.test.mjs tests/production-promotion-guardian.test.mjs tests/hero-media-normalizer.test.mjs tests/hero-media-executor.test.mjs tests/paginacontrole-concurrent-push.test.mjs tests/paginacontrole-concurrency.test.mjs tests/repo-writeback-safety.test.mjs tests/repo-writer-governance.test.mjs tests/repo-writer-candidate.test.mjs tests/menu-writer-candidate-mode.test.mjs tests/regulation-writer-candidate-mode.test.mjs tests/seo-writer-candidate-mode.test.mjs tests/approved-central-blog-candidate-mode.test.mjs tests/blog-update-writer-candidate-mode.test.mjs tests/weekblog-writer-candidate-mode.test.mjs tests/paginacontrole-writer-candidate-mode.test.mjs tests/repository-protection-readiness.test.mjs tests/repository-writer-structural-parity.test.mjs tests/repo-writer-shadow.test.mjs tests/repo-writer-operational-harness.test.mjs tests/content-writer-operational-fixture.test.mjs tests/whole-brain-obligation-contract.test.mjs tests/agent-fabric.test.mjs tests/agent-fabric-gateway.test.mjs tests/agent-fabric-team-memory.test.mjs tests/brain-chat-learning-complete.test.mjs tests/instagram-chat-learning-contract.test.mjs tests/chat-learning-completeness-addendum.test.mjs tests/brain-chat-learning-canonical-closure.test.mjs tests/brain-chat-learning-completeness.test.mjs tests/*guard*.test.mjs tests/delivery-preflight-completion.test.mjs tests/live-preview-trigger-scope.test.mjs tests/brain-cost-membership.test.mjs tests/cost-component-catalog.test.mjs'] });
const COMPONENT_FOUNDATION_COMMAND = Object.freeze({ id: 'component-foundation-contracts', lane: 'shared', command: 'node', args: ['--test', 'tests/parallel-ownership.test.mjs', 'tests/change-classes.test.mjs', 'tests/page-composition.test.mjs', 'tests/component-preview-composition.test.mjs', 'tests/component-boundaries.test.mjs', 'tests/change-scope.test.mjs', 'tests/component-hash-protection.test.mjs', 'tests/detect-changed-components.test.mjs', 'tests/component-preview-workflow-contract.test.mjs'] });

const WEBSITE_COMMANDS = Object.freeze([
  { id: 'website-node-browser-runtime', lane: 'website', command: 'npm', args: ['install', '--no-save', 'playwright@1.55.0'] },
  { id: 'website-node-browser-deps', lane: 'website', command: 'npx', args: ['playwright', 'install', '--with-deps', 'chromium'] },
  { id: 'website-python-browser-runtime', lane: 'website', command: 'python', args: ['-m', 'pip', 'install', 'playwright==1.55.0'], when: 'not-fast-fix' },
  { id: 'website-python-browser-deps', lane: 'website', command: 'python', args: ['-m', 'playwright', 'install', '--with-deps', 'chromium'], when: 'not-fast-fix' },
  { id: 'website-baseline', lane: 'website', command: 'node', args: ['--test', 'tests/site-baseline-guardian.test.mjs', 'tests/site-shell-public-route-inventory.test.mjs', 'tests/site-shell-website-release-risk.test.mjs', 'tests/targeted-website-route-regression.test.mjs'] },
  { id: 'website-shell-components', lane: 'website', command: 'node', args: ['tools/site-shell/test-shell-components.mjs'] },
  { id: 'website-pricing-boundary', lane: 'website', command: 'node', args: ['--test', 'tests/homepage-pricing-boundary-readback.test.mjs'] },
  { id: 'website-static', lane: 'website', command: 'node', when: 'not-fast-fix', args: ['--test', 'tests/v18-vergelijker.test.mjs', 'tests/site-shell-v18-view-integrity.test.mjs', 'tests/seo-native-money-presentation.test.mjs', 'tests/seo-homepage-platform-expertise-toggle.test.mjs', 'tests/standalone-content-page-router-isolation.test.mjs', 'tests/v18-megamenu-heading-contract.test.mjs', 'tests/seo-homepage-context-slider-readability.test.mjs'] },
  { id: 'website-page-check', lane: 'website', command: 'python', when: 'not-fast-fix', args: ['.github/scripts/paginacontrole.py'] },
  { id: 'website-seo-check', lane: 'website', command: 'python', when: 'not-fast-fix', args: ['.github/scripts/seocontrole.py'] },
  { id: 'website-full-regression', lane: 'website', command: 'node', when: 'high-risk', args: ['--test', 'tests/seo-ui-visual-regression-contract.test.mjs', 'tests/seo-ui-visual-regression-geometry.test.mjs', 'tests/seo-ui-visual-regression-browser.test.mjs'] },
  { id: 'website-build-auth', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/bouw-powerhouse-auth.mjs'] },
  { id: 'website-build-knowledge', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/bouw-kennisindex.mjs'] },
  { id: 'website-build-v18-production', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/bouw-v18-production.mjs'] },
  { id: 'website-build-tabs', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/apply-tabbladen.mjs'] },
  { id: 'website-build-views', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/bouw-v18-views.mjs'] },
  { id: 'website-build-chrome', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/bouw-v18-chrome-alles.mjs'] },
  { id: 'website-pricing-rewrite', lane: 'website', command: 'node', when: 'high-risk', env: { BG_PRICING_STAGE: 'rewrite' }, args: ['tools/prijzen-uit-de-homepage.mjs'] },
  { id: 'website-pricing-normalize', lane: 'website', command: 'node', when: 'high-risk', env: { BG_PRICING_STAGE: 'normalize' }, args: ['tools/prijzen-uit-de-homepage.mjs'] },
  { id: 'website-shell-gate', lane: 'website', command: 'node', when: 'high-risk', args: ['tools/site-shell/diagnose-shell-gate.mjs'] },
  { id: 'website-pricing-verify', lane: 'website', command: 'node', when: 'high-risk', env: { BG_PRICING_STAGE: 'verify' }, args: ['tools/prijzen-uit-de-homepage.mjs'] },
  { id: 'website-release-evidence', lane: 'website', command: 'node', when: 'high-risk', env: { CONTEXT: 'github-pull-request' }, args: ['tools/bouw-release-evidence.mjs'] },
  { id: 'website-targeted-browser', lane: 'website', command: 'node', requiresPreview: true, args: ['tools/site-shell/verify-targeted-website-routes.mjs'] },
  { id: 'website-public-visibility', lane: 'website', command: 'node', requiresPreview: true, args: ['tools/site-shell/standalone-visibility-check.mjs'] },
  { id: 'website-megamenu-browser', lane: 'website', command: 'node', when: 'high-risk', requiresPreview: true, args: ['tools/site-shell/v18-megamenu-browser-check.mjs'] },
  { id: 'website-visual-browser', lane: 'website', command: 'node', when: 'high-risk', requiresPreview: true, args: ['tools/site-shell/ui-visual-regression/browser-check.mjs'] },
  { id: 'website-slider-browser', lane: 'website', command: 'node', when: 'high-risk', requiresPreview: true, args: ['tools/site-shell/homepage-context-slider-browser-check.mjs'] },
  { id: 'website-toggle-browser', lane: 'website', command: 'node', when: 'high-risk', requiresPreview: true, args: ['tools/site-shell/homepage-toggle-browser-check.mjs'] },
]);

function uniqueCommands(commands) { const seen = new Set(); return commands.filter(command => { if (seen.has(command.id)) throw new Error(`duplicate single-flight command id: ${command.id}`); seen.add(command.id); return true; }); }
function touchesAny(paths, rules) { return paths.some(path => rules.some(rule => rule.endsWith('/') ? path.startsWith(rule) : path === rule || path.startsWith(rule))); }
function needsSharedMemory(paths, suites) { return suites.backend || suites.automation || touchesAny(paths, ['AGENTS.md', 'config/', 'docs/brain/', 'scripts/brain/', 'brain/', 'tools/brain-', 'tools/delivery-', 'tests/brain-', 'tests/delivery-']); }
function needsComponentFoundation(paths) { return touchesAny(paths, ['config/', 'components/', 'pages/', 'preview/', 'tools/compose-site.mjs', 'tools/compose-component-preview.mjs', 'tools/verify-component-', 'tools/verify-change-scope.mjs', 'tools/detect-changed-components.mjs', 'tests/parallel-ownership.test.mjs', 'tests/change-classes.test.mjs', 'tests/page-composition.test.mjs', 'tests/component-', '.github/workflows/component-preview.yml', '.github/workflows/component-foundation-tdd.yml']); }
export function assertCurrentHead({ plannedHeadSha, observedHeadSha }) { const planned = String(plannedHeadSha ?? '').trim(); const observed = String(observedHeadSha ?? '').trim(); if (!planned || !observed || planned !== observed) throw new Error(`STALE_HEAD_SUPERSEDED: planned=${planned || 'missing'} observed=${observed || 'missing'}`); return true; }
export function createSingleFlightPlan({ baseSha, headSha, prNumber, changedPaths = [], policy }) { const normalizedHead = String(headSha ?? '').trim(); const normalizedBase = String(baseSha ?? '').trim(); const normalizedPr = Number(prNumber); if (!/^[a-f0-9]{12,40}$/i.test(normalizedHead)) throw new TypeError('valid headSha is required'); if (!/^[a-f0-9]{12,40}$/i.test(normalizedBase)) throw new TypeError('valid baseSha is required'); if (!Number.isInteger(normalizedPr) || normalizedPr < 0) throw new TypeError('valid prNumber is required'); const delivery = createDeliveryPlan({ changedPaths, headSha: normalizedHead, policy }); const lanes = delivery.lanes.map(lane => lane.id); const suites = deriveRequiredTestSuites({ lanes }); const commands = [...SHARED_COMMANDS]; if (needsSharedMemory(changedPaths, suites)) commands.push(SHARED_MEMORY_COMMAND); if (needsComponentFoundation(changedPaths)) commands.push(COMPONENT_FOUNDATION_COMMAND); if (suites.backend) commands.push(...BACKEND_COMMANDS); if (suites.portal) commands.push(...PORTAL_COMMANDS); if (suites.automation) commands.push(...AUTOMATION_COMMANDS); if (suites.website) commands.push(...WEBSITE_COMMANDS); return Object.freeze({ version: 'single-flight-v1', baseSha: normalizedBase, headSha: normalizedHead, prNumber: normalizedPr, lanes: Object.freeze(lanes), suites, commands: Object.freeze(uniqueCommands(commands).map(command => Object.freeze({ ...command, args: Object.freeze([...command.args]), env: command.env ? Object.freeze({ ...command.env }) : undefined }))) }); }
function argValue(args, name, fallback = '') { const index = args.indexOf(name); return index >= 0 ? args[index + 1] : fallback; }
function gitDiffNames(range) { return execFileSync('git', ['diff', '--name-only', range], { encoding: 'utf8' }).split(/\r?\n/).filter(Boolean); }
async function websiteRiskFor(changedPaths) { const riskConfig = JSON.parse(await readFile('site/website-release-risk.json', 'utf8')); const acceptedBaseline = JSON.parse(await readFile('site/accepted-baseline.json', 'utf8')); const result = classifyWebsiteRelease({ changedPaths, riskConfig, acceptedBaseline }); let routes = [...result.affected_routes]; if (routes.length === 0) routes = result.lane === 'high-risk' ? ['/', '/ai-act', '/benchmark'] : ['/']; return Object.freeze({ ...result, affected_routes: Object.freeze(routes) }); }
function shouldRun(command, riskLane) { if (!command.when) return true; if (command.when === 'high-risk') return riskLane === 'high-risk'; if (command.when === 'not-fast-fix') return riskLane !== 'fast-fix'; return true; }
async function waitForPreview({ headSha, repository, token }) { if (!repository || !token) throw new Error('PREVIEW_READBACK_REQUIRES_GITHUB_TOKEN_AND_REPOSITORY'); const endpoint = `https://api.github.com/repos/${repository}/commits/${headSha}/status`; const headers = { Authorization: `Bearer ${token}`, Accept: 'application/vnd.github+json', 'X-GitHub-Api-Version': '2022-11-28' }; const deadline = Date.now() + 10 * 60 * 1000; while (Date.now() < deadline) { const response = await fetch(endpoint, { headers }); if (!response.ok) throw new Error(`GitHub status API ${response.status}`); const body = await response.json(); const status = (body.statuses || []).find(item => item.context === 'netlify/bedrijfsgeheugen/deploy-preview'); if (status?.state === 'success') { process.stdout.write(`${JSON.stringify({ event: 'preview-ready', headSha })}\n`); return; } if (['failure', 'error'].includes(status?.state)) throw new Error(`Netlify preview ${status.state}: ${status.description || ''}`); await new Promise(resolve => setTimeout(resolve, 10000)); } throw new Error(`Timed out waiting for exact deploy preview for ${headSha}`); }
function workflowEscape(value) { return String(value ?? '').replaceAll('%', '%25').replaceAll('\r', '%0D').replaceAll('\n', '%0A'); }
async function executeCommand(command, { plan, websiteRisk }) { const previewUrl = `https://deploy-preview-${plan.prNumber}--bedrijfsgeheugen.netlify.app`; const routesJson = JSON.stringify(websiteRisk?.affected_routes || ['/']); const env = { ...process.env, ...(command.env || {}), HEAD_SHA: plan.headSha, EXPECTED_COMMIT: plan.headSha, COMMIT_REF: plan.headSha, BASE_URL: previewUrl, BASELINE_URL: 'https://www.bedrijfsgeheugen.nl', UI_VR_BASE_URL: previewUrl, ROUTES_JSON: routesJson }; process.stdout.write(`${JSON.stringify({ event: 'single-flight-command', id: command.id, lane: command.lane, headSha: plan.headSha, riskLane: websiteRisk?.lane ?? null })}\n`); const result = spawnSync(command.command, command.args, { encoding: 'utf8', env, maxBuffer: 64 * 1024 * 1024, stdio: ['inherit', 'pipe', 'pipe'] }); if (result.stdout) process.stdout.write(result.stdout); if (result.stderr) process.stderr.write(result.stderr); if (result.error) throw result.error; if (result.status !== 0) { const stderrTail = String(result.stderr || result.stdout || '').slice(-4000); const diagnostic = { version: 'single-flight-failure-v1', headSha: plan.headSha, prNumber: plan.prNumber, id: command.id, lane: command.lane, exitCode: result.status, stderrTail }; await writeFile(FAILURE_DIAGNOSTIC_PATH, `${JSON.stringify(diagnostic, null, 2)}\n`, 'utf8'); const detail = `id=${command.id}; lane=${command.lane}; exit=${result.status}; stderr=${stderrTail || 'no captured output'}`; process.stderr.write(`::error title=Single-flight command failed::${workflowEscape(detail)}\n`); throw new Error(`SINGLE_FLIGHT_COMMAND_FAILED:${command.id}:exit=${result.status}`); } }
async function buildRuntimePlan({ base, head, pr }) { const resolvedHead = execFileSync('git', ['rev-parse', head], { encoding: 'utf8' }).trim(); const resolvedBase = execFileSync('git', ['rev-parse', base], { encoding: 'utf8' }).trim(); const changedPaths = gitDiffNames(`${resolvedBase}...${resolvedHead}`); const policy = JSON.parse(await readFile('config/brain-delivery-system.json', 'utf8')); const plan = createSingleFlightPlan({ baseSha: resolvedBase, headSha: resolvedHead, prNumber: Number(pr), changedPaths, policy }); return { plan, changedPaths }; }
async function main() { const [command = 'plan', ...args] = process.argv.slice(2); const base = argValue(args, '--base', 'HEAD^'); const head = argValue(args, '--head', 'HEAD'); const pr = argValue(args, '--pr', '0'); const { plan, changedPaths } = await buildRuntimePlan({ base, head, pr }); const observedHead = execFileSync('git', ['rev-parse', 'HEAD'], { encoding: 'utf8' }).trim(); assertCurrentHead({ plannedHeadSha: plan.headSha, observedHeadSha: observedHead }); const websiteRisk = plan.suites.website ? await websiteRiskFor(changedPaths) : null; if (command === 'plan') { process.stdout.write(`${JSON.stringify({ ...plan, websiteRisk }, null, 2)}\n`); return; } if (command !== 'run') throw new Error(`unknown command: ${command}`); let previewVerified = false; for (const item of plan.commands) { if (!shouldRun(item, websiteRisk?.lane)) continue; assertCurrentHead({ plannedHeadSha: plan.headSha, observedHeadSha: execFileSync('git', ['rev-parse', 'HEAD'], { encoding: 'utf8' }).trim() }); if (item.requiresPreview && !previewVerified) { await waitForPreview({ headSha: plan.headSha, repository: process.env.GITHUB_REPOSITORY, token: process.env.GH_TOKEN || process.env.GITHUB_TOKEN }); previewVerified = true; } await executeCommand(item, { plan, websiteRisk }); } process.stdout.write(`${JSON.stringify({ ok: true, version: plan.version, headSha: plan.headSha, prNumber: plan.prNumber, lanes: plan.lanes, websiteRisk })}\n`); }
if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) { main().catch(error => { process.stderr.write(`${JSON.stringify({ ok: false, error: error.message })}\n`); process.exitCode = 1; }); }
