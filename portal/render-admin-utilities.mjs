const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const delta=n=>`${Number(n)>0?'+':''}${Number(n)||0}`;
function importPreview(p){return`<div class="card" data-import-preview><div class="card-head"><h2>Controleer de wijzigingen</h2><span class="status-pill">Nog niet toegepast</span></div><div class="governance-stats"><div><b>${p.companyChanged?'Ja':'Nee'}</b><span>Bedrijfsnaam wijzigt</span></div><div><b>${delta(p.actionDelta)}</b><span>Acties</span></div><div><b>${delta(p.decisionDelta)}</b><span>Besluiten</span></div><div><b>${delta(p.memoryDelta)}</b><span>Kennisitems</span></div></div><p class="muted">${p.nextCompany?`Doelbedrijf: ${esc(p.nextCompany)}. `:''}Er is nog niets opgeslagen. Pas na bevestiging wordt de back-up naar de beveiligde servercontext geschreven en teruggelezen ter verificatie.</p><div class="button-row"><button class="control-btn" data-portal-action="cancel-import">Annuleer</button><button class="primary-btn" data-portal-action="confirm-import">Bevestig import</button></div></div>`}
export function renderAdminUtilities(route='admin',{pendingImport=null,importError=''}={}){if(route==='admin/legacy/downloaden')return`<section class="page"><div class="page-head"><div><h1>Downloaden</h1><p>Bewaar een actuele JSON-back-up van de bedrijfscontext op je eigen apparaat.</p></div></div><div class="card"><div class="card-head"><h2>Portaalback-up</h2><span class="verified-pill">Lokaal bestand</span></div><p class="muted">De download bevat de huidige portalstate die je op dit moment mag zien. Er wordt niets extern gedeeld.</p><div class="button-row"><button class="primary-btn" data-portal-action="download-state">Download JSON-back-up ⇩</button></div></div></section>`;if(route==='admin/legacy/openen')return`<section class="page"><div class="page-head"><div><h1>Openen</h1><p>Open een eerder gemaakte portaalback-up met preview en expliciete bevestiging vóór enige wijziging.</p></div></div><div class="card"><div class="card-head"><h2>Back-up kiezen</h2><span class="verified-pill">Veilige import</span></div><p class="muted">Ondersteunt de huidige v4-back-up en bestaande v1-back-ups uit het oorspronkelijke portaal. Het bestand wordt eerst lokaal gevalideerd.</p>${importError?`<div class="empty-search">${esc(importError)}</div>`:''}<input id="portal-import-file" type="file" accept="application/json,.json" hidden><div class="button-row"><button class="primary-btn" data-portal-action="select-import">Kies JSON-back-up</button></div></div>${pendingImport?importPreview(pendingImport.preview):''}</section>`;if(route==='admin/legacy/afdrukken')return`<section class="page"><div class="page-head"><div><h1>Afdrukken</h1><p>Gebruik de browser om het huidige portaal af te drukken of als PDF op te slaan.</p></div></div><div class="card"><div class="card-head"><h2>Afdrukken / PDF</h2><span class="verified-pill">Browserfunctie</span></div><p class="muted">De printweergave gebruikt de huidige, toegankelijke portalcontext.</p><div class="button-row"><button class="primary-btn" data-portal-action="print">Afdrukken of opslaan als PDF</button></div></div></section>`;return''}
