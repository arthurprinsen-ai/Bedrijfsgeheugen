import { LEGACY_WORKSPACES } from './core.mjs';
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function legacyWorkspaceFromRoute(route=''){const id=String(route).split('/legacy/')[1]||'';return LEGACY_WORKSPACES.find(x=>x.id===id)||null}
export function renderLegacyBridge({route,customerSlug=''}={}){const workspace=legacyWorkspaceFromRoute(route);if(!workspace)return'';const q=new URLSearchParams();if(customerSlug)q.set('klant',customerSlug);const src=`/klantportaal.html${q.toString()?`?${q}`:''}`;return `<section class="legacy-bridge"><div class="page-title-row"><div><p class="eyebrow">Bestaande werkruimte · ${esc(workspace.group)}</p><h1>${esc(workspace.label)}</h1><p>Deze functie blijft volledig beschikbaar terwijl hij stap voor stap native in het nieuwe portaal wordt opgenomen.</p></div><a class="control-btn" href="${esc(src)}" target="_blank" rel="noopener">Open los ↗</a></div><div class="legacy-bridge-note"><strong>Geen functieverlies.</strong> De oorspronkelijke klantwerkruimte draait hieronder op dezelfde origin en behoudt bestaande data, acties en bediening.</div><iframe class="legacy-frame" data-legacy-workspace="${esc(workspace.id)}" title="${esc(workspace.label)}" src="${esc(src)}" loading="eager"></iframe></section>`}
