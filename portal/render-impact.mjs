export function renderImpact(vm,route='impact'){const money=n=>new Intl.NumberFormat('nl-NL',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(n);return`<section class="page"><div class="page-head"><div><h1>${route.includes('reports')?'Rapportages':'Impact'}</h1><p>Waarde telt pas mee als het resultaat met evidence is gerealiseerd.</p></div><button class="control-btn">Exporteer rapport ⇩</button></div><div class="value-hero-grid"><div class="verified-value-card"><small>Verified Value Created</small><strong>${money(vm.verifiedTotal)}</strong><span>alleen Realised + evidence</span></div><div class="value-funnel">${vm.stages.map(s=>{const items=vm.items.filter(i=>i.stage===s);return`<div><small>${s}</small><b>${money(items.reduce((a,x)=>a+x.amount,0))}</b><span>${items.length} items</span></div>`}).join('')}</div></div><div class="two-col"><div class="card"><div class="card-head"><h2>Impact deze maand</h2></div>${vm.monthly.map(x=>`<div class="list-row"><span>${x.label}</span><b>${x.value}</b><small>${x.delta}</small></div>`).join('')}</div><div class="card"><div class="card-head"><h2>Value pipeline</h2></div>${vm.items.map(x=>`<div class="list-row"><span>${x.label}</span><b>${money(x.amount)}</b><span class="status-pill">${x.stage}</span></div>`).join('')}</div></div></section>`}
