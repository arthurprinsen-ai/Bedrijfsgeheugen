export function renderMemory(state){return`<section class="page"><div class="page-head"><div><h1>Geheugen & kennis</h1><p>Waarom is dit zo? Vind kennis, besluiten, veranderingen en bewijs in één tijdlijn.</p></div><button class="primary-btn" data-ai-prompt="Waarom werken we op deze manier?">✦ Vraag Bedrijfsgeheugen</button></div><div class="memory-search card"><input placeholder="Zoek in kennis, besluiten, lessons, experts en documenten…"><button class="primary-btn">Zoeken</button></div><div class="two-col"><div class="card"><div class="card-head"><h2>Bedrijfstijdlijn</h2></div>${state.memories.map(m=>`<article class="memory-item"><small>${m.type} · ${m.date}</small><h3>${m.title}</h3><p>${m.excerpt}</p><span>${m.evidence} evidence items</span></article>`).join('')}</div><div class="card"><div class="card-head"><h2>Recente agentactiviteit</h2></div>${state.audit.map(a=>`<div class="list-row"><span><b>${a.actor}</b><br><small>${a.time}</small></span><strong>${a.event}</strong><span class="verified-pill">${a.status}</span></div>`).join('')}</div></div></section>`}
