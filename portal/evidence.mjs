export function confidenceLabel(score){const n=Number(score);if(!Number.isFinite(n))return'Unknown';if(n>=90)return'Verified';if(n>=75)return'High confidence';if(n>=50)return'Medium confidence';if(n>0)return'Low confidence';return'Unknown'}
export function normalizeEvidence(input={}){return Object.freeze({id:String(input.id||''),source:String(input.source||'Unknown source'),sourceType:String(input.sourceType||'internal'),retrievedAt:input.retrievedAt||null,confidence:Number(input.confidence||0),confidenceLabel:confidenceLabel(input.confidence),verified:Boolean(input.verified),aiGenerated:Boolean(input.aiGenerated),model:input.model||null})}
