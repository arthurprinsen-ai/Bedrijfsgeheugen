export function renderDecisions(state){return`<section class="page"><div class="page-head"><div><h1>Besluiten</h1><p>Van context en scenario naar eigenaar, uitvoering en gemeten uitkomst.</p></div><button class="primary-btn" data-detail="D-284">Open hoogste prioriteit</button></div><div class="subtabs">${['Te besluiten','In voorbereiding','Besloten','Evalueren'].map((x,i)=>`<button class="${i===0?'is-active':''}">${x}</button>`).join('')}</div><div class="card table-card"><table><thead><tr><th>Besluit</th><th>Fase</th><th>Waarom nu</th><th>Impact</th><th>Confidence</th><th>Eigenaar</th></tr></thead><tbody>${state.decisions.map(d=>`<tr><td><button class="table-link" data-detail="${d.id}"><b>${d.id}</b> · ${d.title}</button></td><td><span class="status-pill">${d.stage}</span></td><td>${d.why}</td><td><b>${d.expected}</b></td><td>${d.confidence}%</td><td>${d.owner}</td></tr>`).join('')}</tbody></table></div></section>`}
