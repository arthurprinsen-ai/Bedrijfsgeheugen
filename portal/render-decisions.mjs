const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function renderDecisions(state){const decisions=Array.isArray(state.decisions)?state.decisions:[];const first=decisions[0];return`<section class="page"><div class="page-head"><div><h1>Besluiten</h1><p>Van context en scenario naar eigenaar, uitvoering en gemeten uitkomst.</p></div>${first?`<button class="primary-btn" data-detail="${esc(first.id)}">Open eerste besluit</button>`:''}</div><div class="subtabs">${['Te besluiten','In voorbereiding','Besloten','Evalueren'].map((x,i)=>`<button class="${i===0?'is-active':''}">${x}</button>`).join('')}</div><div class="card table-card">${decisions.length?`<table><thead><tr><th>Besluit</th><th>Fase</th><th>Waarom nu</th><th>Impact</th><th>Confidence</th><th>Eigenaar</th></tr></thead><tbody>${decisions.map(d=>`<tr><td><button class="table-link" data-detail="${esc(d.id)}"><b>${esc(d.id)}</b> · ${esc(d.title)}</button></td><td><span class="status-pill">${esc(d.stage)}</span></td><td>${esc(d.why)||'—'}</td><td><b>${esc(d.expected)||'—'}</b></td><td>${Number.isFinite(Number(d.confidence))?`${Number(d.confidence)}%`:'—'}</td><td>${esc(d.owner)||'Niet toegewezen'}</td></tr>`).join('')}</tbody></table>`:'<div class="empty-search">Nog geen besluiten uit een live bron vastgelegd.</div>'}</div></section>`}
