import { canCompleteAction } from './core.mjs';
export function renderExecution(state,route='execution'){return`<section class="page"><div class="page-head"><div><h1>Acties & uitvoering</h1><p>Besluiten worden pas waarde na uitvoering, verificatie en resultaatmeting.</p></div><button class="control-btn" data-ai-prompt="Welke actie blokkeert de meeste waarde?">✦ Vind blokkade</button></div><div class="subtabs">${['Acties','Initiatieven','Roadmap','Changes','Automatiseringen','Agents'].map((x,i)=>`<button class="${route.includes(x.toLowerCase())||i===0?'is-active':''}">${x}</button>`).join('')}</div><div class="card table-card"><table><thead><tr><th>Actie</th><th>Eigenaar</th><th>Status</th><th>Deadline</th><th>Bron</th><th>Definition of Done</th></tr></thead><tbody>${state.actions.map(a=>`<tr><td><button class="table-link" data-detail="${a.id}">${a.title}</button></td><td>${a.owner||'<b class="danger-text">Ontbreekt</b>'}</td><td><span class="status-pill">${a.status}</span></td><td>${a.due}</td><td>${a.source}</td><td>${canCompleteAction(a)?'<span class="verified-pill">Verified done</span>':`${a.executed?'✓':'○'} uitvoering · ${a.verified?'✓':'○'} verificatie · ${a.result?'✓':'○'} resultaat`}</td></tr>`).join('')}</tbody></table></div><div class="card agent-loop-card"><div class="card-head"><h2>Agent execution loop</h2><span class="muted">policy-bound</span></div><div class="pipeline">${['Observer','Analyst','Advisor','Policy','Operator','Guardian','Librarian'].map((x,i)=>`<span class="${i<3?'done':''}">${x}</span>`).join('')}</div></div></section>`}
