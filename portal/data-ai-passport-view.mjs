import { buildPassportFromState } from './data-ai-passport.mjs';
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const STATUS_LABELS=Object.freeze({verified:'Geverifieerd',partially_verified:'Gedeeltelijk geverifieerd',unknown:'Nog te bewijzen',action_required:'Actie nodig',effective:'Effectief',evidence_missing:'Evidence ontbreekt',remediation_required:'Herstel nodig',not_applicable:'Niet van toepassing',future_readiness:'Toekomstige verplichting',not_determined:'Niet bepaald'});
const fact=(l,v)=>`<div class="passport-fact"><span>${esc(l)}</span><strong>${esc(v||'Nog te bewijzen')}</strong></div>`;
const getControl=(p,id)=>p.controls.find(x=>x.id===id)||{};
const flowStep=({id,index,label,kicker,detail,status='unknown',active=false})=>`<button type="button" class="passport-flow-node status-${esc(status)}" data-flow-step="${esc(id)}" aria-pressed="${active?'true':'false'}"><span class="flow-index">0${index}</span><span class="flow-pulse" aria-hidden="true"></span><strong>${esc(label)}</strong><small>${esc(kicker)}</small><span class="flow-status">${esc(STATUS_LABELS[status]||status)}</span><span class="flow-hidden" data-flow-detail>${esc(detail)}</span></button>`;
export function renderDataAiPassport(state={}){const passport=buildPassportFromState(state),{summary,technicalFacts={}}=passport,aiAct=passport.aiAct||{},company=technicalFacts.tenantOwner||state?.company?.name||'Uw organisatie',residency=getControl(passport,'data-residency'),models=getControl(passport,'model-register'),transfer=getControl(passport,'cross-border-transfer'),audit=getControl(passport,'monitoring-audit'),oversight=getControl(passport,'human-oversight');const steps=[{id:'source',index:1,label:'Klantbron',kicker:'ERP · documenten · systemen',detail:`Data-eigenaar: ${company}. Alleen gekoppelde bronnen komen de tenantcontext binnen.`,status:'unknown',active:true},{id:'tenant',index:2,label:'Bedrijfsgeheugen',kicker:'Tenant · identiteit · autorisatie',detail:`Runtime: ${technicalFacts.hostingProvider||'Nog te bewijzen'} · API verwerking: ${technicalFacts.processingRegion||'Nog te bewijzen'}.`,status:residency.status||'unknown'},{id:'workflow',index:3,label:'Workflow',kicker:'Orchestratie · minimale context',detail:'Automatisering verwerkt alleen benodigde context; leverancier en route blijven onderdeel van het bewijsregister.',status:getControl(passport,'supplier-assurance').status||'unknown'},{id:'ai',index:4,label:'AI',kicker:'Model · provider · use-case',detail:`${models.claim||'AI-provider en model nog te bewijzen.'} ${transfer.claim||''}`.trim(),status:models.status||'unknown'},{id:'storage',index:5,label:'Opslag',kicker:technicalFacts.storageRegion||'Regio nog te bewijzen',detail:`${technicalFacts.stateStore||'Opslag nog te bewijzen'} · ${technicalFacts.storageRegion||'locatie nog te bewijzen'}.`,status:residency.status||'unknown'},{id:'audit',index:6,label:'Audit',kicker:'Evidence · menselijke regie',detail:`${audit.claim||'Auditbewijs nog te bewijzen'} ${oversight.claim||''}`.trim(),status:audit.status||'unknown'}];const actControls=Array.isArray(aiAct.controls)?aiAct.controls:[],findings=Array.isArray(aiAct.findings)?aiAct.findings:[];return `<section class="passport-page"><header class="passport-head"><div><a class="passport-back" href="/portal/index.html#/company/data">← Terug naar Data & systemen</a><p class="eyebrow">DATA · AI · EU AI ACT ASSURANCE</p><h1>Volg uw data.<br><em>Bewijs elke verplichting.</em></h1><p>Een levende evidence-laag voor data, AI en de EU AI Act. Geen zelfcertificering: iedere conclusie is scope- en bewijsgebonden.</p></div><div class="passport-score"><span class="score-orbit"></span><strong>${summary.evidenceCoveragePct}%</strong><span>evidence coverage</span><small>${findings.length} open AI Act finding(s)</small></div></header>
<section class="passport-mode-switch" role="tablist" aria-label="Passport modus"><button type="button" role="tab" aria-selected="true" data-passport-mode="data-flow">Data Flow</button><button type="button" role="tab" aria-selected="false" data-passport-mode="ai-act-audit">AI Act Audit</button><a class="primary-btn" href="/portal/eu-ai-act-audit-report.html">Open EU AI Act audit report →</a></section>
<section class="passport-livebar">${fact('Data-eigenaar / tenant',company)}${fact('API verwerking',technicalFacts.processingRegion)}${fact('Portal-state opslag',technicalFacts.storageRegion?`${technicalFacts.stateStore} · ${technicalFacts.storageRegion}`:technicalFacts.stateStore)}${fact('AI Act baseline',aiAct.regulatoryBaseline?.id)}${fact('Assurance',aiAct.summary?.conclusion)}</section>
<section class="passport-orbit" data-mode-panel="data-flow"><div class="orbit-head"><div><p class="eyebrow">LIVE TRUST JOURNEY</p><h2>Van bron naar bewijs</h2></div></div><div class="passport-flow">${steps.map(flowStep).join('<span class="flow-link" aria-hidden="true"><i></i></span>')}</div><div class="flow-inspector" aria-live="polite"><div><span class="inspector-kicker">01 · KLANTBRON</span><h3 id="flow-detail-title">Klantbron</h3></div><p id="flow-detail-copy">${esc(steps[0].detail)}</p></div><div class="trust-boundary"><span>EU DATA ANCHOR</span><strong>${esc(technicalFacts.storageRegion||'Nog te bewijzen')}</strong><i></i><span>PROCESSING REALITY</span><strong>${esc(transfer.claim||technicalFacts.processingRegion||'Nog te bewijzen')}</strong></div></section>
<section class="passport-orbit ai-act-audit" data-mode-panel="ai-act-audit" hidden><div class="orbit-head"><div><p class="eyebrow">EU AI ACT CONTROL ROOM</p><h2>Verplichting → control → evidence → finding</h2></div><p>${esc(aiAct.summary?.conclusion||'Scope nog niet bepaalbaar.')}</p></div><div class="ai-act-radar">${actControls.map((c,i)=>`<button type="button" class="ai-act-node status-${esc(c.status)}" data-ai-act-control="${esc(c.id)}"><span>${esc(c.article)}</span><strong>${esc(c.title)}</strong><small>${esc(c.name||c.useCaseId)} · ${esc(STATUS_LABELS[c.status]||c.status)}</small><em>${esc(c.effectiveFrom)}</em></button>`).join('')}</div><aside class="ai-act-inspector" aria-live="polite"><p class="eyebrow">EVIDENCE INSPECTOR</p><h3>Audit trail</h3><p>Selecteer een Article 4, Article 5, Article 50 of high-risk readiness control om scope, status en evidence te onderzoeken.</p><strong>Open findings: ${findings.length}</strong>${findings.length?`<ul>${findings.map(f=>`<li>${esc(f.article)} · ${esc(f.title)} · ${esc(f.owner)}</li>`).join('')}</ul>`:''}</aside></section>
<div class="passport-summary"><div><strong>${summary.verified}</strong><span>Geverifieerd</span></div><div><strong>${summary.partiallyVerified}</strong><span>Gedeeltelijk</span></div><div><strong>${summary.unknown}</strong><span>Nog te bewijzen</span></div><div><strong>${summary.actionRequired}</strong><span>Actie nodig</span></div></div><p class="passport-disclaimer">Dit Passport en auditrapport zijn evidence-statusoverzichten, geen juridisch certificaat of blanket EU AI Act complianceverklaring.</p><div class="passport-grid">${passport.controls.map(c=>`<article class="passport-card status-${esc(c.status)}"><div class="passport-card-head"><span>${esc(c.category)}</span><b>${esc(STATUS_LABELS[c.status]||c.status)}</b></div><h2>${esc(c.label)}</h2><p>${esc(c.description)}</p><dl><div><dt>Eigenaar</dt><dd>${esc(c.owner||'Niet vastgelegd')}</dd></div><div><dt>Evidence</dt><dd>${c.verifiedEvidenceCount}/${c.evidenceCount} geverifieerd</dd></div><div><dt>Wat weten we?</dt><dd>${esc(c.claim||'Geen geverifieerde claim')}</dd></div></dl></article>`).join('')}</div></section>`;}
