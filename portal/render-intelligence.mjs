import { SIGNAL_PIPELINE } from './core.mjs';
const esc=v=>String(v??'');
export function renderIntelligence(state,route='intelligence'){return`<section class="page"><div class="page-head"><div><h1>AI & Insights</h1><p>Van extern signaal naar gevalideerde impact op jouw bedrijf.</p></div><button class="primary-btn" data-ai-prompt="Welke ontwikkeling heeft nu de hoogste impact?">✦ Prioriteer met AI</button></div><div class="subtabs">${['Feed','Markt','Concurrenten','Klanten','Technologie','Wetgeving','Trends','Kansen','Risico’s'].map((x,i)=>`<button class="${i===0?'is-active':''}">${x}</button>`).join('')}</div><section class="card"><div class="card-head"><h2>Intelligence pipeline</h2><span class="muted">Detect → onderbouw → handel</span></div><div class="pipeline">${SIGNAL_PIPELINE.map((x,i)=>`<span class="${i<4?'done':''}">${i+1}. ${x}</span>`).join('')}</div>${state.signals.map(s=>`<article class="signal-row"><div><small>${s.category} · ${s.source}</small><h3>${esc(s.title)}</h3><p>${esc(s.summary)}</p><div class="pill-row">${s.affected.map(a=>`<span>${a}</span>`).join('')}</div></div><div class="signal-meta"><b>${s.confidence}%</b><small>confidence</small><span class="impact-badge">${s.impact}</span><button class="text-link" data-detail="${s.id}">Bekijk impact →</button></div></article>`).join('')}</section></section>`}
