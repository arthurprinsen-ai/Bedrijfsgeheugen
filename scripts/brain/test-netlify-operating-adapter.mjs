import {createBlobAdapter} from '../../brain/operating-loop/netlify-adapter.mjs';
const values=new Map();const blobStore={async get(k){return values.get(k)||null;},async setJSON(k,v){values.set(k,v);},async list({prefix}){return {blobs:[...values.keys()].filter(k=>k.startsWith(prefix)).map(key=>({key}))};}};const adapter=createBlobAdapter(blobStore);await adapter.put('tenant/records/1',{x:1});await adapter.put('tenant/records/2',{x:2});if((await adapter.get('tenant/records/1')).x!==1) throw new Error('get/put failed');const list=await adapter.list('tenant/records/');if(list.length!==2||list.some(x=>!x.value)) throw new Error('list hydrate failed');console.log('netlify operating adapter tests passed');
