import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('config/brain-source-mappings.json','utf8'));if(c.contract!=='BRAIN-SOURCE-MAPPING-v1') throw new Error('wrong source mapping contract');for(const p of ['make','notion','supabase','dataforseo','github','netlify']){const x=c.sources?.[p];if(!x) throw new Error(`missing ${p}`);if(x.mapping_version!=='v1'||x.preserve_raw!==true||x.unknown_field_policy!=='preserve') throw new Error(`${p} compatibility mapping invalid`);if(!Array.isArray(x.canonical_types)||x.canonical_types.length<1) throw new Error(`${p} canonical types missing`);}for(const t of ['Evidence','Signal','Opportunity','Decision','Action','Outcome','Learning','Entity','Relation']) if(!c.allowed_canonical_types.includes(t)) throw new Error(`canonical type ${t} missing`);if(c.default_unknown_source!=='fail_closed') throw new Error('unknown source must fail closed');console.log('source mapping tests passed');
