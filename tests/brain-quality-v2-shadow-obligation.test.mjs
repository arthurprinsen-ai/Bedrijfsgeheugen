import test from 'node:test';import assert from 'node:assert/strict';import {evaluateShadowObservation} from '../scripts/brain/quality/production-shadow.mjs';test('shadow drift preserves evidence and open RCA/regression/prevention',()=>{const r=evaluateShadowObservation({id:'contract',required:true,expected:'a',observed:'b',evidence:'probe-42'});assert.equal(r.obligation.evidence,'probe-42');assert.equal(r.obligation.root_cause,'OPEN');assert.equal(r.obligation.regression_test,'OPEN');assert.equal(r.obligation.prevention_rule,'OPEN');});
