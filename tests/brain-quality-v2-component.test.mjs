import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2-component.json','utf8'));test('v2 component cannot promote before protected closure',()=>{assert.equal(c.lifecycle,'active_candidate');assert.match(c.promotion_rule,/exact-head/);assert.match(c.promotion_rule,/protected merge/);assert.match(c.promotion_rule,/current-main readback/);});
