import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));test('v2 closed loop contains defect regression learning and adoption',()=>{for(const x of ['ESCAPED_DEFECT','ROOT_CAUSE','REGRESSION','TEST_VALUE','LEARN','SCOUT','BENCHMARK','ADOPT_REJECT'])assert.ok(c.closed_loop.includes(x),x);});
