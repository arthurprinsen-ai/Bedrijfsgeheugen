import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));test('all ten v2 capabilities are explicitly represented',()=>assert.equal(Object.keys(c.capabilities).length,10));test('implementation contract has no missing file references',()=>{for(const p of Object.values(c.implementation))assert.equal(fs.existsSync(p),true,p);});
