import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));test('v2 CI contract exposes one deterministic fast command',()=>{assert.equal(c.ci_contract.fast_command,'node --test tests/brain-quality-v2-all.test.mjs');assert.equal(c.ci_contract.validator_command,'node scripts/brain/quality/validate-v2.mjs');assert.equal(c.ci_contract.integration_status,'candidate_until_existing_workflows_execute_exact_head');});
