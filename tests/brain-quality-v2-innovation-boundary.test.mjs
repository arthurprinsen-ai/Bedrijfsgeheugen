import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));test('frontier innovations require benchmark and cannot auto-adopt',()=>{assert.equal(c.ai_policy.may_auto_adopt,false);assert.equal(c.deep_sensors.performance_profiling,'benchmark_before_adoption');assert.equal(c.capabilities.performance_root_cause.experimental_profilers_require_benchmark,true);});
