import test from 'node:test';import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';test('v2 deterministic validator reports READY',()=>{const r=spawnSync(process.execPath,['scripts/brain/quality/validate-v2.mjs'],{encoding:'utf8'});assert.equal(r.status,0,r.stderr||r.stdout);const out=JSON.parse(r.stdout);assert.equal(out.status,'READY');assert.deepEqual(out.gaps,[]);});
