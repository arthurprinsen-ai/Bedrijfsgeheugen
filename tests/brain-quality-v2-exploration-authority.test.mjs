import test from 'node:test';import assert from 'node:assert/strict';import {normalizeFinding} from '../scripts/brain/quality/exploration-policy.mjs';test('exploration finding identifies v2 but has no release authority',()=>{const f=normalizeFinding({source:'agent',kind:'layout'});assert.equal(f.authority,'powerhouse-quality-intelligence-v2');assert.equal(f.release_evidence,false);assert.equal(f.requires_deterministic_reproduction,true);});
