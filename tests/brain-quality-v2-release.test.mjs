import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2-release.json','utf8'));test('release candidate is not prematurely live',()=>{assert.equal(r.status,'CANDIDATE');assert.ok(r.required_evidence.includes('protected merge'));assert.ok(r.required_evidence.includes('current-main readback'));assert.ok(r.forbidden_claims.some(x=>x.includes('LIVE & BEWEZEN')));});
