import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2-release.json','utf8'));test('release candidate is not prematurely live',()=>{assert.equal(r.status,'CANDIDATE');assert.equal(r.source_status,'implemented_candidate');assert.equal(r.source_implementation,'powerhouse-quality-intelligence-v2');assert.equal(r.website_runtime_change,false);assert.equal(r.release_scope,'quality assurance source/CI authority only');assert.ok(r.required_evidence.includes('protected merge'));assert.ok(r.required_evidence.includes('current-main readback'));assert.ok(r.forbidden_claims.some(x=>x.includes('LIVE & BEWEZEN')));assert.ok(r.open_obligations.length>0);assert.ok(r.deep_sensor_obligations.length>=5);});
