import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));const r=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2-release.json','utf8'));test('provenance capability requires exact SHA but is not falsely proven',()=>{assert.equal(c.capabilities.build_provenance_sbom.exact_sha_required,true);assert.match(c.deep_sensors.provenance_sbom,/eligible_artifact/);assert.ok(r.deep_sensor_obligations.some(x=>x.includes('provenance/SBOM')));});
