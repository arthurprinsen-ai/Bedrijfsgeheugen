import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const s=JSON.parse(fs.readFileSync('config/powerhouse-quality-surfaces.json','utf8'));test('canonical v2 surface inventory contains public portal cockpit and backend',()=>{const ids=s.surfaces.map(x=>x.id);for(const id of ['public-website','portal-v2','cockpit','brain-backend'])assert.ok(ids.includes(id),id);});test('optional API and integration targets stay optional until registered',()=>{for(const id of ['openapi','postgres-integration'])assert.equal(s.surfaces.find(x=>x.id===id).required,false);});
