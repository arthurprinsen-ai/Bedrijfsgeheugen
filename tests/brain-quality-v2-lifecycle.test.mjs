import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const impl=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));const component=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2-component.json','utf8'));const release=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2-release.json','utf8'));test('source can be active while release component remains candidate',()=>{assert.equal(impl.lifecycle,'active');assert.equal(component.lifecycle,'active_candidate');assert.equal(release.status,'CANDIDATE');});
