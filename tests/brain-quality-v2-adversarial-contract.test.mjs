import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const m=JSON.parse(fs.readFileSync('config/powerhouse-quality-adversarial-matrix.json','utf8'));test('adversarial contract includes tenant identity idempotency and provider failure',()=>{const ids=m.cases.map(x=>x.id);for(const x of ['duplicate-idempotency','malformed-expired-identity','tenant-boundary-idor-bola','provider-partial-failure'])assert.ok(ids.includes(x),x);});test('all adversarial cases require safe target registration',()=>{assert.equal(m.production_destructive,false);for(const c of m.cases){assert.equal(c.required_when_registered,true);assert.ok(c.safe_target);}});
