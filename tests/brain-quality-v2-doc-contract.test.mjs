import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const d=fs.readFileSync('docs/powerhouse/POWERHOUSE_QUALITY_INTELLIGENCE_V2.md','utf8');test('human docs preserve deterministic authority and deep-sensor evidence boundary',()=>{assert.match(d,/never replaces/);assert.match(d,/does \*\*not\*\* mean they have executed successfully/);assert.match(d,/NOT_REGISTERED/);assert.match(d,/BRAIN-CLOSED-LOOP-v1/);});
