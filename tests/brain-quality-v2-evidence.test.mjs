import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));test('only proven is green evidence',()=>{assert.deepEqual(c.evidence_states.green,['proven']);assert.equal(c.evidence_states.advisory,'never_release_evidence');});test('unknown and not registered fail closed when required',()=>{assert.equal(c.evidence_states.unknown,'blocking_when_required');assert.equal(c.evidence_states.not_registered,'blocking_when_required');});
