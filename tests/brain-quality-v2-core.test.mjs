import test from 'node:test';import assert from 'node:assert/strict';import * as q from '../scripts/brain/quality/quality-v2-core.mjs';test('v2 core exposes one canonical fingerprint and sensors',()=>{assert.equal(q.QUALITY_V2_FINGERPRINT,'powerhouse-quality-intelligence-v2');for(const fn of ['buildCoverageReport','evaluateExplorationAction','normalizeFinding','evaluateShadowObservation','recommendLane'])assert.equal(typeof q[fn],'function');});
