import test from 'node:test';import assert from 'node:assert/strict';import {buildCoverageReport} from '../scripts/brain/quality/coverage-intelligence.mjs';test('required missing coverage emits deduplicable canonical fingerprint ingredients',()=>{const r=buildCoverageReport({surfaces:[{id:'portal',required:true,authority:'portal-v2-canonical-surface-v1'}],evidence:[]});assert.equal(r.status,'BLOCKED');assert.equal(r.obligations[0].kind,'quality_coverage_gap');assert.equal(r.obligations[0].surface_id,'portal');assert.equal(r.obligations[0].authority,'portal-v2-canonical-surface-v1');});
