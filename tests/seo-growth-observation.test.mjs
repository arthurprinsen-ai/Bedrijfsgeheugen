import test from 'node:test';
import assert from 'node:assert/strict';
import { normalizeBehaviorEvent, normalizeSearchMetric, normalizeOutcome } from '../tools/seo-growth/normalize-observation.mjs';
import { toBg211Envelope } from '../tools/seo-growth/bg211-envelope.mjs';

test('behavior and search normalize into one growth observation model',()=>{const b=normalizeBehaviorEvent({event_id:'e1',occurred_at:'2026-09-05T20:00:00Z',event_type:'primary_cta_click',canonical:'https://www.bedrijfsgeheugen.nl/prijzen',intent_id:'kosten digitalisering mkb',keyword_cluster:'kosten digitalisering mkb',attribution_root:'s1'});const s=normalizeSearchMetric({period:'2026-09-05',query:'kosten digitalisering mkb',canonical:'https://www.bedrijfsgeheugen.nl/prijzen',intent_id:'kosten digitalisering mkb',keyword_cluster:'kosten digitalisering mkb',impressions:100,clicks:5,ctr:.05,position:4});assert.equal(b.schema_version,'seo-growth-observation-v1');assert.equal(s.schema_version,b.schema_version);assert.equal(b.canonical,s.canonical);assert.match(b.fingerprint,/^seo-growth\|/);});
test('PII fields are rejected',()=>{assert.throws(()=>normalizeBehaviorEvent({event_id:'e',event_type:'page_view',canonical:'https://www.bedrijfsgeheugen.nl/',intent_id:'x',email:'a@b.nl'}),/PII/);assert.throws(()=>normalizeOutcome({event_type:'lead_outcome',canonical:'https://www.bedrijfsgeheugen.nl/',intent_id:'x',attribution_root:'r',name:'Arthur'}),/PII/);});
test('growth observation becomes canonical BG211 envelope',()=>{const o=normalizeBehaviorEvent({event_id:'e2',event_type:'page_view',canonical:'https://www.bedrijfsgeheugen.nl/',intent_id:'digitalisering mkb',keyword_cluster:'digitalisering mkb',attribution_root:'s2'});const e=toBg211Envelope(o,'2026-09-05T20:00:00Z');assert.equal(e.producer_id,'seo-growth-observation');assert.equal(e.domain,'growth');assert.equal(e.privacy_class,'non-pii-analytics');assert.ok(e.fingerprint);});
