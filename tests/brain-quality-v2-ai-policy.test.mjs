import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('powerhouse/assurance/quality-intelligence-v2.json','utf8'));test('AI can propose tests but cannot approve or waive',()=>{assert.equal(c.ai_policy.may_generate_candidate_tests,true);assert.equal(c.ai_policy.release_authority,false);assert.equal(c.ai_policy.may_waive_gate,false);assert.equal(c.ai_policy.may_auto_adopt,false);assert.equal(c.ai_policy.deterministic_reproduction_required,true);});
