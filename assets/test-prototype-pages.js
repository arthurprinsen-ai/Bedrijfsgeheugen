/* Recovered test-prototype pages intentionally use the shared V18 header and
   assets/js/menu.js as the single owner of mobile navigation.  Keep this file
   as a compatibility marker only: creating a second drawer here caused the
   recovered routes to diverge from the production navigation contract. */
(()=>{})();
