(()=>{const nav=document.querySelector('header .nav');const source=document.querySelector('header .links');if(!nav||!source)return;const button=document.createElement('button');button.type='button';button.className='mobile-menu-button';button.textContent='Menu';button.setAttribute('aria-expanded','false');button.setAttribute('aria-label','Menu openen');const drawer=document.createElement('aside');drawer.className='mobile-drawer';drawer.setAttribute('aria-hidden','true');const close=document.createElement('button');close.type='button';close.textContent='×';close.setAttribute('aria-label','Menu sluiten');drawer.append(close);[...source.querySelectorAll('a')].forEach(a=>{const copy=a.cloneNode(true);if(copy.classList.contains('cta'))copy.classList.remove('cta');copy.innerHTML=`<span>${copy.textContent.trim()}</span><span aria-hidden="true">→</span>`;drawer.append(copy)});function set(open){drawer.classList.toggle('open',open);drawer.setAttribute('aria-hidden',String(!open));button.setAttribute('aria-expanded',String(open));button.textContent=open?'Sluit':'Menu';document.body.style.overflow=open?'hidden':''}button.addEventListener('click',()=>set(button.getAttribute('aria-expanded')!=='true'));close.addEventListener('click',()=>set(false));drawer.addEventListener('click',e=>{if(e.target.closest('a'))set(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape')set(false)});nav.append(button);document.body.append(drawer)})();