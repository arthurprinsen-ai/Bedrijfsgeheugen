# Portal V2 Project Navigation Summary

This branch turns `Project` into the canonical Portal V2 entry for project work, adds the approved five project groups, renders a fail-closed project cockpit, exposes both project levels on desktop, and preserves the existing V2 router/state as the only source of truth.
