# Portal V2 Project Navigation Delivery Contract

Do not claim live or complete until all required CI checks are green on one exact head SHA, the branch is merged, production serves the merge SHA, and production readback verifies the Project entry and project context on mobile and desktop.
