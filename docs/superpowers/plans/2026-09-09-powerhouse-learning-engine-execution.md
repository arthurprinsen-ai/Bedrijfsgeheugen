# Powerhouse Learning Engine execution mode

Execution is inline on the isolated feature branch `feature/powerhouse-learning-engine`, using test-first commits and existing GitHub/Netlify CI as the executable verification environment because the local container cannot resolve GitHub DNS. No production code is added before RED is observed in CI.
