# SEO validator registry semantics

The legacy audit now treats only each registry entry's primary keyword as the exclusive literal claim. Canonical `bg-keyword-cluster` metadata is authoritative when present; title/H1 matching is retained only for legacy pages without that metadata. Secondary keywords remain semantic coverage targets.
