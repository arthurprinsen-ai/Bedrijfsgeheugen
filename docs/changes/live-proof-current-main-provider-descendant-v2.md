# Live proof: current main ↔ provider production

Date: 2026-09-25

Powerhouse terminal delivery requires a fresh binding between repository truth and provider truth immediately before `LIVE_BEWEZEN`.

Exact main/provider SHA equality is sufficient. If provider production is newer than the delivered merge, Git ancestry must prove containment. If current main is newer than provider production, status remains non-terminal until production catches up.

The delivery node also retains ownership through learning, relevant skill projection and existing Powerhouse dashboard/current-state writeback. Pending autonomous work is not returned to the user as a “what now” list.

Fingerprint: `delivery|live-proof|current-main-provider-descendant|v1`.
