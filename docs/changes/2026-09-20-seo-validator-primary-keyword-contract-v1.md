# SEO validator primary keyword contract

The legacy audit now treats only each registry entry's primary keyword as the mandatory title/H1 claim. Secondary keywords remain supporting variants. The audit also reads the current `bg-keyword-cluster` metadata contract, with legacy `bg-zoekwoord` compatibility.
