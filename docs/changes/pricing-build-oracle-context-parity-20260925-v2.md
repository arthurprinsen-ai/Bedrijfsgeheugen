# Pricing build-oracle context parity v2 — 25 september 2026

Netlify production build exit code 2 is isolated to the pre-i18n pricing integrity step. The oracle still requires the retired token `Belangrijkste doel nu`, while the canonical pricing/Portal contract uses `Wat wil je bereiken?` and the `Ondernemersdoelen` group.

The oracle now checks the current canonical terms. No runtime or i18n fail-closed behavior is weakened.
