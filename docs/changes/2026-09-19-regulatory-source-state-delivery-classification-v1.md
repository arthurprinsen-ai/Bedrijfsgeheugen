# Regulatory source-state delivery classification

The canonical `data/regulatory-source-state.json` projection is now classified as automation delivery work, alongside the existing regulatory source monitor, ingest tooling, workflows and regressions.

The change is intentionally narrow: unrelated files under `data/` remain unclassified and fail closed.
