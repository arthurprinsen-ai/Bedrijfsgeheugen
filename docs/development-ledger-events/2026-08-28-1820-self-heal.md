# Bedrijfsgeheugen self-heal ledger events — 2026-08-28 18:20 CEST

These events are append-only operational memory for the canonical development ledger. They were isolated into this companion file because the connector refused the full-file append transaction to `docs/development-ledger.md`; no existing canonical history was overwritten.

## ERROR — browser evidence false auto-repair dispatch
- **Fingerprint:** `browser-gate|about-blank|false-auto-repair-dispatch`
- **Signal:** BG151 delivered `extension-present` and `feed-dom` evidence with `pageUrl=about:blank`; BG152 treated those unattached observations as real runtime/selector failures. Separate scalar evidence `1` was also JSON-parsable and became an empty runtime failure.
- **Impact:** PH09 was invoked without usable grounding, producing `LEARNING_CONTEXT_REQUIRED` outcomes and consuming unnecessary AI credits instead of proving a product defect.
- **Root cause:** BG152 validated JSON syntax but not evidence shape or target-page attachment before repair/promotion routing.
- **Evidence:** PH09 runs `00126c8e08364c538646bfc25c587c87` and `fe2702a1c02b47c8a6102348aeee321c` consumed 19.37 and 19.87 credits; related false-negative run `82daa03b886740729dffb4bd27e33b6f` consumed 56.1 credits.
- **Known failed approach:** interpreting `about:blank` or a scalar payload as sufficient runtime evidence and then asking an AI repair agent to infer missing context.
- **Owner:** QA/Regression + Telemetry/Self-Healing.
- **Last-known-good production:** production remained available throughout; no product rollback was required.
- **Reusable lesson:** collector attachment quality is part of the release contract; invalid evidence must terminate deterministically before AI dispatch.

## RECOVERY — BG152 evidence attachment guard
- **Fingerprint:** `browser-gate|about-blank|inconclusive-guard`
- **Fix:** BG152 module 24 now requires a JSON object and a resolved HTTP(S) `pageUrl` other than `about:blank` before any repair or promotion decision. Invalid/unattached observations return `INCONCLUSIVE`, `agentScenarioId=0`, `repairAllowed=false`.
- **Regression gate:** live contract tests: `9430412457694985a1d1a5924dcc5bd8` (`about:blank` -> `INCONCLUSIVE`), `dd1f5bb9cc354bb795b402f708ece4b4` (scalar `1` -> `INCONCLUSIVE`), `1a6b87daa64a415d9982e13b9e4a898f` (resolved LinkedIn HTTPS PASS -> `PROMOTE_CANDIDATE`).
- **Verification:** all three BG152 runs completed successfully and preserved resolved-page routing while eliminating the false dispatch path.
- **Rollback:** restore the previous deterministic classifier in BG152 module 24.
- **Reusable lesson:** reject unattached evidence before fingerprinting, repair dispatch or release promotion.

## RECOVERY — BG168 production-negation veto
- **Fingerprint:** `shared-learning|bg168|negated-production-veto`
- **Fix:** BG168 module 2 now explicitly vetoes `PRODUCTION_PROMOTION` for `no production promotion`, `no production deploy`, `NO_PRODUCTION_PROMOTION`, `not promoted to production`, `production untouched`, `preview-only`, and `production_promotion=false` before keyword classification.
- **Regression gate:** BG168 run `44074f6195284377a0167b65fafc3d43` classified `RECOVERY complete. Preview only. No production promotion occurred; production untouched.` as `AGENT_RECOVERY`, not `PRODUCTION_PROMOTION`.
- **Verification:** affirmative structured production events still remain routable through the material-outcome path; exact GitHub/Netlify/BG169 evidence remains the production authority.
- **Rollback:** restore the prior BG168 classifier.
- **Reusable lesson:** semantic release words are not release evidence; explicit negation must dominate keyword matching.

## ERROR — synthetic promotion classifier test is non-authoritative
- **Fingerprint:** `shared-learning|bg168|synthetic-promotion-test-non-authoritative`
- **Signal:** affirmative classifier regression run `21e4cd7d6b7e433ab193f1a594e3d8d8` traversed normal BG168/BG166 writeback and created test fingerprint `test|bg168|affirmative-production`.
- **Impact:** a synthetic test record could be mistaken for release truth if exact GitHub/Netlify evidence were ignored.
- **Recovery:** corrective shared-learning event written through BG166; `test|` fingerprints are explicitly non-authoritative for production state.
- **Reusable lesson:** classifier tests that traverse material writeback must be marked non-authoritative or isolated from release memory.

## IMPROVEMENT — deterministic browser-gate credit avoidance
- **Fingerprint:** `browser-gate|inconclusive-guard|credit-avoidance`
- **Baseline:** the observed unattached-evidence pattern consumed 95.34 PH09 credits across two contextless dispatches plus one false-negative repair path.
- **Change:** invalid JSON-object shape and unresolved/about:blank pages now end in a zero-agent deterministic `INCONCLUSIVE` decision.
- **After:** equivalent invalid observations require no repair-agent call; observed false-dispatch credit demand falls from 95.34 credits for that pattern to 0 at the BG152 dispatch boundary.
- **Protected metrics:** valid resolved-page PASS evidence still returns `PROMOTE_CANDIDATE`; security and human-send controls are unchanged.
- **Rollback:** restore the previous BG152 module 24 classifier if legitimate attached evidence is blocked.
- **Reusable lesson:** deterministic evidence validation improves reliability and cost simultaneously.

## PRODUCTION_PROMOTION — verified OpenArt hero media foundation
- **Fingerprint:** `production|openart-hero-foundation|media-ready`
- **Candidate:** PR #111 head `a7aed7805bda929d40016a5bce01ca5630aa2688` added the pinned, iPhone-accepted OpenArt hero derivative and static production verification without changing the current homepage.
- **Evidence:** derivative SHA-256 `a261792e9b0058802ab5b30ce107c7ac14e8b2291a3bd7ee78fdb5968bbe97fd`, 1920x1080, 30fps, H.264/yuv420p, no audio, faststart; physical iPhone/Safari acceptance was bound to that exact derivative in PR #110 and the PR #111 verifier requires that identity.
- **Promotion:** PR #111 merged as `13656ffa21c655d01532c19371dc330545cd6682`; a subsequent SEO-status-only automation commit advanced `main` to `598286ffd842837e0642fcc812ea25225dbcbba8` while retaining the promoted foundation.
- **Production deploy:** Netlify deploy `6a91b67906de610007197eb5`, context `production`, state `ready`, exact `commit_ref=598286ffd842837e0642fcc812ea25225dbcbba8`, published `2026-08-28T16:25:52.247Z`.
- **Smoke/regression:** public `https://www.bedrijfsgeheugen.nl/` returned current homepage content successfully; Netlify processed 68 redirects and 16 header rules without errors, deployed 3 functions + 1 edge function, and secret scan found 0 matches.
- **Protected behavior:** PR #111 explicitly leaves the current homepage unchanged; the promoted asset is a verified media foundation for a separately gated integration.
- **Rollback/last-known-good:** prior production `854e52620604689abc47dfff813b8dea5a3062bb` / deploy `6a91af48168a000008c5a956` remains the rollback reference for this promotion chain.
- **Reusable lesson:** separate media acceptance from homepage integration, bind device acceptance to the exact derivative hash, then verify exact production artifact after promotion.
