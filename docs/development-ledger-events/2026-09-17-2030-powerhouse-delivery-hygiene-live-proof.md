# 2026-09-17 20:30 CEST — RECOVERY + IMPROVEMENT + CONTRACT_CHANGE — Powerhouse Delivery Hygiene LIVE proof

- **Primary fingerprint:** `delivery|control-plane|duplicate-candidates-ci-fanout-moving-main-v1`
- **Related fingerprints:** `delivery|test-oracle|legacy-wiring-assertion-v1`, `delivery|github-api|transient-admission-5xx-v1`, `delivery|workflow-tests|stateful-batch-nondeterminism-v1`, `delivery|merged-branch|exact-head-cleanup-v1`.
- **Signal:** the repository accumulated many concurrent PRs, superseded candidates, duplicate CI on identical SHAs, moving-main churn and expensive workflows that started before a candidate was proven canonical. Product regressions, stale test assumptions and transient GitHub failures were mixed together as generic red CI.
- **Impact:** delivery throughput dropped, runner queues grew, agents could create overlapping recovery lanes, exact-head evidence was harder to interpret and old branches/PRs continued consuming attention after the underlying obligation had moved on.
- **Root cause:** BRAIN delivery already had lane classification, exact-SHA checks and branch hygiene, but repository-wide admission control was missing. CI fan-out occurred before candidate ownership was proven; supersession/WIP/promotion serialization were not one canonical contract; several tests asserted old workflow placement instead of behavior; transient GitHub API failures in admission were treated like deterministic product failures; merged branch cleanup was not part of terminal hygiene.
- **Known failed approaches:** adding cleanup without admission; repeatedly rebasing every time `main` moved; starting successor PRs instead of reusing the canonical obligation lane; treating historical red runs as current failures; fixing test-oracle mismatches by changing correct product wiring; using broad trial-and-error commits to discover a failing batch; assuming a provider/API 503 was a product defect; declaring `LIVE & BEWEZEN` while merged feature branches remained open.
- **Final architecture:** `POWERHOUSE-DELIVERY-HYGIENE-v1` extends the existing control plane rather than introducing a second queue or brain. It enforces machine-readable PR metadata, one active executable candidate per obligation, WIP admission, deterministic supersession, promotion serialization, fail-closed identity checks, cheap admission before expensive CI, lane-aware CI budgeting, a Repository Janitor, BRAIN re-admission before handoff and exact-head merged same-repository branch cleanup.
- **CI acceleration:** broad PR workflows were path-budgeted; the protected `test` context remains the single branch-protection aggregator; irrelevant production/brand/chat/component suites no longer need to run for unrelated delivery-policy changes. Speed comes from less work and deterministic cancellation/dedupe, not from weakening security or release gates.
- **Test-oracle learning:** an older website contract test assumed website checks lived literally in `required-test.yml`; after lane delegation this assertion was obsolete. The test was corrected to verify the canonical boundary in `lane-website.yml`. Product wiring was not regressed to satisfy an obsolete test.
- **Stateful-test learning:** the combined control-plane test batch exposed nondeterministic behavior that did not reproduce when tests ran independently. The stateful batch is serialized where required; product lanes remain concurrent. Test-harness races must be separated from product failures before changing production code.
- **GitHub API resilience learning:** BRAIN pre-handoff admission hit transient HTTP 503 after earlier admission and all lane verification had succeeded. Admission now uses bounded retry for transient 502/503/504/network failures, remains fail-closed after retry exhaustion, and still emits durable failure evidence/fingerprint instead of failing without diagnostic state.
- **Merged-branch hygiene:** PR #1964 added exact merged same-repository branch cleanup. It refuses default-branch deletion, verifies the branch still points at the exact merged head SHA, retries transient API failures, treats already-absent as idempotent success and proves deletion through 404 readback.
- **Regression gates:** delivery-hygiene admission/supersession/WIP tests; Required wiring and aggregator tests; Unified Brain admission/pre-handoff tests; branch-hygiene metadata integration; Repository Janitor safe-action tests; workflow path-budget contracts; merged-branch cleanup contract; existing security, Portal V2, backend, browser/readback, CodeQL, Engineering Intelligence and Supply Chain Trust gates.
- **Candidate proof:** PR #1961 exact candidate reached green protected `test`, BRAIN delivery, browser/readback, CodeQL and supporting trust suites before merge. It merged to `main` as `1dc806ea1baa78616a4ab58c92f7a0b9b9110137`.
- **Cleanup proof:** recovery PR #1964 passed admission, Required `test`, BRAIN, CodeQL and lane checks and merged as `986be03a0a0c7c4417a280cb5a09803b65e25937`. The original `fix/powerhouse-delivery-hygiene-v1` branch was subsequently read back as absent.
- **Canonical documentation proof:** PR #1967 documented the live behavior and merged to protected `main` as `266095257f50e772c9c214676a706ce53504fe02` on 2026-09-17 20:27 CEST. `main` readback confirmed that exact SHA with required status context `test` still enforced.
- **Final branch-cleanup runtime proof:** workflow run `35258967080` (`Powerhouse Merged Branch Cleanup`, run #4) for `docs/powerhouse-delivery-hygiene-v1-readback` completed `success`; this closes the previously queued housekeeping evidence and proves the post-merge cleanup mechanism on a second real branch.
- **Owner:** Architecture/Integrator + Reliability + Knowledge/Governance + QA/Regression.
- **Rollback/last-known-good:** if delivery hygiene itself regresses, disable its admission/janitor/cleanup wiring while retaining existing Required test, Unified BRAIN, branch protection and immutable evidence. Never relax security/integrity gates to recover throughput. Exact-head cleanup must remain disabled rather than delete a branch when identity is ambiguous.
- **Reusable prevention rule:** `ONE_OBLIGATION_ONE_ACTIVE_CANDIDATE_ONE_MERGE_PATH_ONE_PRODUCTION_PROOF_AUTO_CLEANUP_v1`. Before opening work, read canonical obligation state. Reuse the existing lane. Prove admission before expensive CI. Re-check admission before handoff. Treat old red runs, transient provider/API errors and test-oracle defects as separate failure classes. Merge only exact-head green candidates. Terminal `LIVE & BEWEZEN` includes canonical writeback and deterministic cleanup of superseded/temporary delivery state.
- **Reusable lesson:** throughput is improved by reducing competing work, not by removing safeguards. The control plane must decide ownership and identity before expensive execution; tests must assert behavior rather than historical wiring; transient infrastructure errors need bounded fail-closed recovery with evidence; and repository hygiene is part of Definition of Done, not optional cleanup after delivery.
