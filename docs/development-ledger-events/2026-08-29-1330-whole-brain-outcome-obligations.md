# 2026-08-29 13:30 CEST — CONTRACT_CHANGE + PRODUCTION_PROMOTION — Whole-Brain Outcome Obligations

- **Fingerprint:** `whole-brain|outcome-obligations|no-silent-failure`
- **Signal:** a scheduled social publication was expected on 29 August, while the native publisher scenarios returned technical success after a zero-candidate Notion search and therefore produced no publication side effect. The system conflated run success with outcome success.
- **Impact:** any workflow could silently omit an expected business/system result while still appearing green, leaving detection dependent on a human noticing the absence.
- **Root cause:** no generic machine-enforced outcome-obligation contract existed above individual workflow/run statuses. Expected results, deadline, evidence policy, idempotency and recovery ownership were not universally mandatory.
- **Contract:** `docs/outcome-obligations.md`, `config/outcome-obligations.json` and `tools/outcome-obligation-validator.mjs` define the whole-brain invariant: `NO SILENT FAILURE`, `NO LOST OBLIGATION`, `GREEN MEANS OUTCOME VERIFIED`, `RED MEANS AGENTS KEEP WORKING`.
- **Behavior:** an expected due action without outcome evidence becomes `MISSED_OBLIGATION` and `recoveryRequired=true` even when `technicalSuccess=true`. A technical success, empty result set or `zero candidates` can no longer satisfy an expected obligation.
- **Idempotency:** recovery must check existing external evidence/idempotency keys before repeating side effects, preventing duplicate posts, mail, writes, deploys or other actions.
- **Future-agent governance:** `AGENTS.md` and `docs/development-operating-system.md` now require outcome-obligation compliance, reconciliation and `MISSED_OBLIGATION` / `AUTO_REPAIR` writeback for every current and future agent/workflow.
- **TDD RED 1:** Shared Agent Memory Tests run `33250114402` failed on SHA `8e46d78a38d0b29adb497d7e33f06375a5eff9e4` before the evaluator/config/docs existed.
- **TDD GREEN 1:** run `33250148583` passed after adding evaluator/config/docs.
- **TDD RED 2:** run `33250170534` failed after strengthening the test to require whole-agent governance before `AGENTS.md` and the operating system were updated.
- **Exact candidate GREEN:** run `33250210649` passed on candidate SHA `850463dc6af92a86a68267f03d46f6c0f3fd3a52`.
- **Promotion:** PR #156 merged exact candidate head into `main` as `8f791e53c6cb90450ace832b066894b37ca02ce3`.
- **Production deploy:** Netlify deploy `6a92c29d64972b000858a180`, state `ready`, context `production`, exact `commit_ref=8f791e53c6cb90450ace832b066894b37ca02ce3`, published `2026-08-29T11:29:55.010Z`.
- **Protected deploy evidence:** 68 redirects and 16 headers processed without errors; 3 functions and 1 edge function deployed; secret scan found 0 matches.
- **Open runtime boundary:** the Make ChatGPT connector currently returns `400 — We couldn't connect your account`. Live verification of BG184/BG156 therefore remains an explicit `BLOCKED_HARD_BOUNDARY` for credentials/account connection, already fingerprinted as `make|connector|account-connect-400`; it is not allowed to be reported as green.
- **Recovery resume:** once Make connectivity returns, the next run must inspect BG184, verify the corrected BG156 `type`/`source` envelope, reconcile all overdue obligations and continue GREEN-UNTIL-DONE without duplicate side effects.
- **Owner:** Knowledge & Governance + Telemetry/Self-Healing + Architect/Integrator.
- **Rollback:** revert PR #156/merge SHA if the generic contract itself causes an unexpected governance regression; previous production SHA is the parent `1c727040912829c3d34657f635bc7a2d99aac66f`.
- **Reusable lesson:** workflow execution is implementation detail; acceptance belongs to the intended outcome. Absence is a first-class failure whenever an outcome was expected.
