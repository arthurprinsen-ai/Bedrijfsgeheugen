# SEO-controle operational verification canary

Purpose: trigger the canonical repository-writer operational verification workflow for `seo-controle` from an isolated verification branch.

This file is a non-production verification marker only. The writer workflow must create a candidate PR and pass immutable shadow verification before migration state may advance.
