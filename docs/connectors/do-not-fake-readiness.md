# Do not fake readiness

A connector must never report configured merely because Bedrijfsgeheugen can construct a request. Configured state requires the explicit server-side provider switch; live operational proof remains separate.
