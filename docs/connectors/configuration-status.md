# Configuration status semantics

`configured` means a server-side provider endpoint is present and the runtime can attempt a safe test. It does not mean external credentials, permissions, or upstream availability have been independently proven. Production readback remains the authority for the Bedrijfsgeheugen endpoint itself.
