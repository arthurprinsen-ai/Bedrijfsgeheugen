# Implementation note

The remaining code gap is optional server document extraction. AFAS and Exact already use explicit server-side safe-test endpoint switches; do not fake configured state when those switches are absent.
