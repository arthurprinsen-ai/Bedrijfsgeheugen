# Fail-closed behavior

Without a configured server provider, AFAS, Exact and document extraction must remain unavailable for real execution. Safe-test samples may use explicit `extractedFields`; that path is test evidence only and must not be interpreted as a configured document-extraction provider.
