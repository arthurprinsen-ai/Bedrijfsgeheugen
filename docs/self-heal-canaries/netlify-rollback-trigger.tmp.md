# Temporary Netlify rollback trigger

Transient docs-only file used to emit a normal GitHub push event after a history-preserving rollback. It is deleted immediately; the final tree must equal the verified last-known-good tree.
